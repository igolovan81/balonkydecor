# Admin Internationalisation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add CZ/SK/EN/UA/RU language switching to the admin portal, starting with navigation links and the Settings page.

**Architecture:** A new `AdminLangMiddleware` attaches an `I18n` instance (reading from `lang/admin/{lang}.json`) to every admin request. `AdminBaseController::renderAdmin()` picks it up and registers the `t()` Twig function, identical to the public-site pattern. Lang preference is stored per-user in the `users.lang` DB column and cached in `$_SESSION['admin_lang']` for the duration of the login session.

**Tech Stack:** PHP 8, Slim 4, Twig 3, PDO/MySQL 8, slim/psr7, PHPUnit 11.

## Global Constraints

- `lang/admin/` files must have exactly the same 28 keys in every language file.
- `AdminLangMiddleware` is applied only to the `/admin` group (not globally).
- Flash messages in PHP controllers stay hardcoded Czech — out of scope.
- All admin templates except `admin-base.twig` and `admin/settings/index.twig` stay untouched.
- Migration file must be `V006__admin_lang.sql` (V001–V005 already exist).
- Docker MySQL must be running (`docker compose up -d`) before running tests.

---

## File Map

| Action | Path | Responsibility |
|--------|------|----------------|
| Create | `database/migrations/V006__admin_lang.sql` | Add `lang` column to `users` |
| Modify | `src/Models/AdminUserModel.php` | Add `getLang()` + `setLang()` |
| Create | `tests/Unit/Models/AdminUserModelTest.php` | Test getLang/setLang |
| Create | `lang/admin/cs.json` | Czech admin strings |
| Create | `lang/admin/sk.json` | Slovak admin strings |
| Create | `lang/admin/en.json` | English admin strings |
| Create | `lang/admin/uk.json` | Ukrainian admin strings |
| Create | `lang/admin/ru.json` | Russian admin strings |
| Create | `src/Middleware/AdminLangMiddleware.php` | Attach admin I18n to request |
| Create | `tests/Unit/Middleware/AdminLangMiddlewareTest.php` | Test middleware behaviour |
| Modify | `src/routes.php` | Add middleware + set-lang route |
| Modify | `src/Controllers/Admin/AdminBaseController.php` | Register I18nExtension in renderAdmin() |
| Create | `src/Controllers/Admin/AdminLangController.php` | Handle lang switch action |
| Modify | `src/Controllers/Admin/AuthController.php` | Write admin_lang to session on login |
| Modify | `templates/layout/admin-base.twig` | t() nav + lang switcher |
| Modify | `templates/admin/settings/index.twig` | t() all labels |
| Modify | `www/assets/css/admin.css` | Lang switcher styles |

---

## Task 1: DB Migration + AdminUserModel

**Files:**
- Create: `database/migrations/V006__admin_lang.sql`
- Modify: `src/Models/AdminUserModel.php`
- Create: `tests/Unit/Models/AdminUserModelTest.php`

**Interfaces:**
- Produces: `AdminUserModel::getLang(int $id): string`, `AdminUserModel::setLang(int $id, string $lang): void`

- [ ] **Step 1: Create the migration file**

`database/migrations/V006__admin_lang.sql`:
```sql
ALTER TABLE users ADD COLUMN lang VARCHAR(5) NOT NULL DEFAULT 'cs';
```

- [ ] **Step 2: Apply migration to local DB**

```bash
docker compose up -d
mysql -h 127.0.0.1 -u balonky -pbalonky balonkydecor < database/migrations/V006__admin_lang.sql
```

Expected: no output (success).

- [ ] **Step 3: Write the failing tests**

`tests/Unit/Models/AdminUserModelTest.php`:
```php
<?php
namespace Tests\Unit\Models;

use App\Models\AdminUserModel;
use PHPUnit\Framework\TestCase;

class AdminUserModelTest extends TestCase
{
    private static int $userId;

    public static function setUpBeforeClass(): void
    {
        $hash = password_hash('testpassword', PASSWORD_BCRYPT);
        self::$userId = AdminUserModel::create('langtest@example.com', $hash, 'editor');
    }

    public static function tearDownAfterClass(): void
    {
        AdminUserModel::delete(self::$userId);
    }

    public function test_getLang_defaults_to_cs(): void
    {
        $lang = AdminUserModel::getLang(self::$userId);
        $this->assertSame('cs', $lang);
    }

    public function test_setLang_persists_to_db(): void
    {
        AdminUserModel::setLang(self::$userId, 'en');
        $this->assertSame('en', AdminUserModel::getLang(self::$userId));
    }

    public function test_getLang_returns_cs_for_unknown_id(): void
    {
        $this->assertSame('cs', AdminUserModel::getLang(999999));
    }
}
```

- [ ] **Step 4: Run tests — verify they fail**

```bash
php vendor/bin/phpunit tests/Unit/Models/AdminUserModelTest.php --testdox
```

Expected: FAIL — `Call to undefined method AdminUserModel::getLang()`

- [ ] **Step 5: Add getLang() and setLang() to AdminUserModel**

Add to the end of `src/Models/AdminUserModel.php`, before the closing `}`:

```php
    public static function getLang(int $id): string
    {
        $pdo  = Database::getConnection();
        $stmt = $pdo->prepare('SELECT lang FROM users WHERE id = ? LIMIT 1');
        $stmt->execute([$id]);
        return $stmt->fetchColumn() ?: 'cs';
    }

    public static function setLang(int $id, string $lang): void
    {
        $pdo  = Database::getConnection();
        $pdo->prepare('UPDATE users SET lang = ? WHERE id = ?')
            ->execute([$lang, $id]);
    }
```

- [ ] **Step 6: Run tests — verify they pass**

```bash
php vendor/bin/phpunit tests/Unit/Models/AdminUserModelTest.php --testdox
```

Expected: PASS — 3 tests, 3 assertions.

- [ ] **Step 7: Commit**

```bash
git add database/migrations/V006__admin_lang.sql src/Models/AdminUserModel.php tests/Unit/Models/AdminUserModelTest.php
git commit -m "feat: add lang column to users and AdminUserModel::getLang/setLang"
```

---

## Task 2: Admin Lang JSON Files

**Files:**
- Create: `lang/admin/cs.json`
- Create: `lang/admin/sk.json`
- Create: `lang/admin/en.json`
- Create: `lang/admin/uk.json`
- Create: `lang/admin/ru.json`

**Interfaces:**
- Produces: 28-key JSON files under `lang/admin/` consumed by `I18n` service

- [ ] **Step 1: Create the directory**

```bash
mkdir -p lang/admin
```

- [ ] **Step 2: Create lang/admin/cs.json**

```json
{
  "nav.dashboard": "Dashboard",
  "nav.products": "Produkty",
  "nav.categories": "Kategorie",
  "nav.orders": "Objednávky",
  "nav.gallery": "Galerie",
  "nav.blog": "Blog",
  "nav.pages": "Stránky",
  "nav.settings": "Nastavení",
  "nav.users": "Uživatelé",
  "nav.logout": "Odhlásit se",
  "settings.title": "Nastavení",
  "settings.web": "Web",
  "settings.site_name": "Název webu",
  "settings.contact_email": "Kontaktní e-mail",
  "settings.phone": "Telefon",
  "settings.smtp": "SMTP (odesílání e-mailů)",
  "settings.smtp_host": "SMTP host",
  "settings.smtp_port": "SMTP port",
  "settings.smtp_user": "SMTP uživatel",
  "settings.smtp_pass": "SMTP heslo",
  "settings.smtp_from": "Odesílatel (From e-mail)",
  "settings.gopay": "GoPay",
  "settings.gopay_hint": "Nechte GoID prázdné pro vývojový bypass (objednávka se ihned označí jako zaplacená).",
  "settings.gopay_go_id": "GoPay GoID",
  "settings.client_id": "Client ID",
  "settings.client_secret": "Client Secret",
  "settings.test_mode": "Testovací režim (sandbox)",
  "settings.save": "Uložit nastavení"
}
```

- [ ] **Step 3: Create lang/admin/sk.json**

```json
{
  "nav.dashboard": "Dashboard",
  "nav.products": "Produkty",
  "nav.categories": "Kategórie",
  "nav.orders": "Objednávky",
  "nav.gallery": "Galéria",
  "nav.blog": "Blog",
  "nav.pages": "Stránky",
  "nav.settings": "Nastavenie",
  "nav.users": "Používatelia",
  "nav.logout": "Odhlásiť sa",
  "settings.title": "Nastavenie",
  "settings.web": "Web",
  "settings.site_name": "Názov webu",
  "settings.contact_email": "Kontaktný e-mail",
  "settings.phone": "Telefón",
  "settings.smtp": "SMTP (odosielanie e-mailov)",
  "settings.smtp_host": "SMTP host",
  "settings.smtp_port": "SMTP port",
  "settings.smtp_user": "SMTP používateľ",
  "settings.smtp_pass": "SMTP heslo",
  "settings.smtp_from": "Odosielateľ (From e-mail)",
  "settings.gopay": "GoPay",
  "settings.gopay_hint": "Nechajte GoID prázdne pre vývojový bypass (objednávka sa ihneď označí ako zaplatená).",
  "settings.gopay_go_id": "GoPay GoID",
  "settings.client_id": "Client ID",
  "settings.client_secret": "Client Secret",
  "settings.test_mode": "Testovací režim (sandbox)",
  "settings.save": "Uložiť nastavenie"
}
```

- [ ] **Step 4: Create lang/admin/en.json**

```json
{
  "nav.dashboard": "Dashboard",
  "nav.products": "Products",
  "nav.categories": "Categories",
  "nav.orders": "Orders",
  "nav.gallery": "Gallery",
  "nav.blog": "Blog",
  "nav.pages": "Pages",
  "nav.settings": "Settings",
  "nav.users": "Users",
  "nav.logout": "Log out",
  "settings.title": "Settings",
  "settings.web": "Website",
  "settings.site_name": "Site name",
  "settings.contact_email": "Contact email",
  "settings.phone": "Phone",
  "settings.smtp": "SMTP (email sending)",
  "settings.smtp_host": "SMTP host",
  "settings.smtp_port": "SMTP port",
  "settings.smtp_user": "SMTP user",
  "settings.smtp_pass": "SMTP password",
  "settings.smtp_from": "Sender (From email)",
  "settings.gopay": "GoPay",
  "settings.gopay_hint": "Leave GoID empty for dev bypass (order is immediately marked as paid).",
  "settings.gopay_go_id": "GoPay GoID",
  "settings.client_id": "Client ID",
  "settings.client_secret": "Client Secret",
  "settings.test_mode": "Test mode (sandbox)",
  "settings.save": "Save settings"
}
```

- [ ] **Step 5: Create lang/admin/uk.json**

```json
{
  "nav.dashboard": "Панель",
  "nav.products": "Товари",
  "nav.categories": "Категорії",
  "nav.orders": "Замовлення",
  "nav.gallery": "Галерея",
  "nav.blog": "Блог",
  "nav.pages": "Сторінки",
  "nav.settings": "Налаштування",
  "nav.users": "Користувачі",
  "nav.logout": "Вийти",
  "settings.title": "Налаштування",
  "settings.web": "Сайт",
  "settings.site_name": "Назва сайту",
  "settings.contact_email": "Контактний e-mail",
  "settings.phone": "Телефон",
  "settings.smtp": "SMTP (відправка e-mailів)",
  "settings.smtp_host": "SMTP хост",
  "settings.smtp_port": "SMTP порт",
  "settings.smtp_user": "SMTP користувач",
  "settings.smtp_pass": "SMTP пароль",
  "settings.smtp_from": "Відправник (From e-mail)",
  "settings.gopay": "GoPay",
  "settings.gopay_hint": "Залиште GoID порожнім для dev bypass (замовлення одразу позначається як оплачене).",
  "settings.gopay_go_id": "GoPay GoID",
  "settings.client_id": "Client ID",
  "settings.client_secret": "Client Secret",
  "settings.test_mode": "Тестовий режим (sandbox)",
  "settings.save": "Зберегти налаштування"
}
```

- [ ] **Step 6: Create lang/admin/ru.json**

```json
{
  "nav.dashboard": "Панель",
  "nav.products": "Товары",
  "nav.categories": "Категории",
  "nav.orders": "Заказы",
  "nav.gallery": "Галерея",
  "nav.blog": "Блог",
  "nav.pages": "Страницы",
  "nav.settings": "Настройки",
  "nav.users": "Пользователи",
  "nav.logout": "Выйти",
  "settings.title": "Настройки",
  "settings.web": "Сайт",
  "settings.site_name": "Название сайта",
  "settings.contact_email": "Контактный e-mail",
  "settings.phone": "Телефон",
  "settings.smtp": "SMTP (отправка e-mailов)",
  "settings.smtp_host": "SMTP хост",
  "settings.smtp_port": "SMTP порт",
  "settings.smtp_user": "SMTP пользователь",
  "settings.smtp_pass": "SMTP пароль",
  "settings.smtp_from": "Отправитель (From e-mail)",
  "settings.gopay": "GoPay",
  "settings.gopay_hint": "Оставьте GoID пустым для dev bypass (заказ сразу помечается как оплаченный).",
  "settings.gopay_go_id": "GoPay GoID",
  "settings.client_id": "Client ID",
  "settings.client_secret": "Client Secret",
  "settings.test_mode": "Тестовый режим (sandbox)",
  "settings.save": "Сохранить настройки"
}
```

- [ ] **Step 7: Verify all files are valid JSON and have equal key counts**

```bash
for f in lang/admin/*.json; do echo -n "$f: "; php -r "echo count(json_decode(file_get_contents('$f'), true)) . ' keys\n';"; done
```

Expected output:
```
lang/admin/cs.json: 28 keys
lang/admin/en.json: 28 keys
lang/admin/ru.json: 28 keys
lang/admin/sk.json: 28 keys
lang/admin/uk.json: 28 keys
```

- [ ] **Step 8: Commit**

```bash
git add lang/admin/
git commit -m "feat: add admin lang files for cs/sk/en/uk/ru"
```

---

## Task 3: AdminLangMiddleware

**Files:**
- Create: `src/Middleware/AdminLangMiddleware.php`
- Create: `tests/Unit/Middleware/AdminLangMiddlewareTest.php`

**Interfaces:**
- Consumes: `$_SESSION['admin_lang']`, `lang/admin/{lang}.json` files (from Task 2)
- Produces: request attribute `admin_i18n` → `App\Services\I18n`, request attribute `admin_lang` → string

- [ ] **Step 1: Write the failing tests**

```bash
mkdir -p tests/Unit/Middleware
```

`tests/Unit/Middleware/AdminLangMiddlewareTest.php`:
```php
<?php
namespace Tests\Unit\Middleware;

use App\Middleware\AdminLangMiddleware;
use App\Services\I18n;
use PHPUnit\Framework\TestCase;
use Psr\Http\Message\ResponseInterface;
use Psr\Http\Message\ServerRequestInterface;
use Psr\Http\Server\RequestHandlerInterface;
use Slim\Psr7\Factory\ServerRequestFactory;
use Slim\Psr7\Response;

class AdminLangMiddlewareTest extends TestCase
{
    private string $langDir;
    private AdminLangMiddleware $middleware;

    protected function setUp(): void
    {
        if (session_status() === PHP_SESSION_NONE) {
            session_start();
        }
        $_SESSION = [];
        $this->langDir   = __DIR__ . '/../../../lang/admin';
        $this->middleware = new AdminLangMiddleware($this->langDir);
    }

    private function makeHandler(): object
    {
        return new class implements RequestHandlerInterface {
            public ?ServerRequestInterface $captured = null;
            public function handle(ServerRequestInterface $request): ResponseInterface {
                $this->captured = $request;
                return new Response();
            }
        };
    }

    public function test_defaults_to_cs_when_session_key_absent(): void
    {
        $request = (new ServerRequestFactory())->createServerRequest('GET', '/admin');
        $handler = $this->makeHandler();

        $this->middleware->process($request, $handler);

        $this->assertSame('cs', $handler->captured->getAttribute('admin_lang'));
        $this->assertInstanceOf(I18n::class, $handler->captured->getAttribute('admin_i18n'));
    }

    public function test_uses_lang_from_session(): void
    {
        $_SESSION['admin_lang'] = 'en';
        $request = (new ServerRequestFactory())->createServerRequest('GET', '/admin');
        $handler = $this->makeHandler();

        $this->middleware->process($request, $handler);

        $this->assertSame('en', $handler->captured->getAttribute('admin_lang'));
    }

    public function test_falls_back_to_cs_for_unsupported_lang(): void
    {
        $_SESSION['admin_lang'] = 'de';
        $request = (new ServerRequestFactory())->createServerRequest('GET', '/admin');
        $handler = $this->makeHandler();

        $this->middleware->process($request, $handler);

        $this->assertSame('cs', $handler->captured->getAttribute('admin_lang'));
    }
}
```

- [ ] **Step 2: Run tests — verify they fail**

```bash
php vendor/bin/phpunit tests/Unit/Middleware/AdminLangMiddlewareTest.php --testdox
```

Expected: FAIL — `Class "App\Middleware\AdminLangMiddleware" not found`

- [ ] **Step 3: Create AdminLangMiddleware**

`src/Middleware/AdminLangMiddleware.php`:
```php
<?php
namespace App\Middleware;

use App\Services\I18n;
use Psr\Http\Message\ResponseInterface;
use Psr\Http\Message\ServerRequestInterface;
use Psr\Http\Server\MiddlewareInterface;
use Psr\Http\Server\RequestHandlerInterface;

class AdminLangMiddleware implements MiddlewareInterface
{
    private const SUPPORTED = ['cs', 'sk', 'en', 'uk', 'ru'];

    public function __construct(private string $langDir) {}

    public function process(ServerRequestInterface $request, RequestHandlerInterface $handler): ResponseInterface
    {
        if (session_status() === PHP_SESSION_NONE) {
            session_start();
        }
        $lang = $_SESSION['admin_lang'] ?? 'cs';
        if (!in_array($lang, self::SUPPORTED, true)) {
            $lang = 'cs';
        }
        return $handler->handle(
            $request
                ->withAttribute('admin_i18n', new I18n($lang, $this->langDir))
                ->withAttribute('admin_lang', $lang)
        );
    }
}
```

- [ ] **Step 4: Run tests — verify they pass**

```bash
php vendor/bin/phpunit tests/Unit/Middleware/AdminLangMiddlewareTest.php --testdox
```

Expected: PASS — 3 tests, 3 assertions.

- [ ] **Step 5: Wire AdminLangMiddleware into the admin group in routes.php**

In `src/routes.php`, add the `use` import after the existing `use App\Middleware\AuthMiddleware;` line:

```php
use App\Middleware\AdminLangMiddleware;
```

Then change the closing of the admin group (line 115) from:

```php
})->add(new AuthMiddleware());
```

to:

```php
})->add(new AdminLangMiddleware(__DIR__ . '/../lang/admin'))->add(new AuthMiddleware());
```

- [ ] **Step 6: Run full test suite**

```bash
php vendor/bin/phpunit --testdox
```

Expected: all existing tests still pass + 3 new middleware tests pass.

- [ ] **Step 7: Commit**

```bash
git add src/Middleware/AdminLangMiddleware.php tests/Unit/Middleware/AdminLangMiddlewareTest.php src/routes.php
git commit -m "feat: add AdminLangMiddleware and wire into admin group"
```

---

## Task 4: AdminBaseController Patch

**Files:**
- Modify: `src/Controllers/Admin/AdminBaseController.php`

**Interfaces:**
- Consumes: request attribute `admin_i18n` (I18n instance from Task 3), request attribute `admin_lang` (string from Task 3)
- Produces: `t()` Twig function available in all admin templates; `admin_lang` variable in every template

- [ ] **Step 1: Patch renderAdmin() in AdminBaseController**

Replace the existing `renderAdmin()` method in `src/Controllers/Admin/AdminBaseController.php`:

```php
    protected function renderAdmin(Request $request, Response $response, string $template, array $data = []): Response
    {
        if (session_status() === PHP_SESSION_NONE) {
            session_start();
        }
        $flash = $this->getFlash();
        $i18n  = $request->getAttribute('admin_i18n');
        $env   = $this->twig->getEnvironment();
        if ($i18n && !$env->hasExtension(\App\Twig\I18nExtension::class)) {
            $env->addExtension(new \App\Twig\I18nExtension($i18n));
        }
        return $this->twig->render($response, $template, array_merge([
            'flash'          => $flash,
            'session_role'   => $_SESSION['admin_user']['role']  ?? '',
            'session_email'  => $_SESSION['admin_user']['email'] ?? '',
            'admin_lang'     => $request->getAttribute('admin_lang', 'cs'),
        ], $data));
    }
```

- [ ] **Step 2: Run full test suite — confirm nothing breaks**

```bash
php vendor/bin/phpunit --testdox
```

Expected: all tests pass.

- [ ] **Step 3: Commit**

```bash
git add src/Controllers/Admin/AdminBaseController.php
git commit -m "feat: register I18nExtension in AdminBaseController::renderAdmin()"
```

---

## Task 5: AdminLangController + Route

**Files:**
- Create: `src/Controllers/Admin/AdminLangController.php`
- Modify: `src/routes.php`

**Interfaces:**
- Consumes: `?l=` query param, `$_SESSION['admin_user']['id']`, `AdminUserModel::setLang()` (Task 1)
- Produces: `GET /admin/set-lang?l={lang}` route that persists the chosen lang and redirects

- [ ] **Step 1: Create AdminLangController**

`src/Controllers/Admin/AdminLangController.php`:
```php
<?php
namespace App\Controllers\Admin;

use App\Models\AdminUserModel;
use Psr\Http\Message\ResponseInterface as Response;
use Psr\Http\Message\ServerRequestInterface as Request;

class AdminLangController extends AdminBaseController
{
    private const SUPPORTED = ['cs', 'sk', 'en', 'uk', 'ru'];

    public function setLang(Request $request, Response $response, array $args): Response
    {
        if (session_status() === PHP_SESSION_NONE) {
            session_start();
        }
        $lang   = $request->getQueryParams()['l'] ?? '';
        $userId = (int) ($_SESSION['admin_user']['id'] ?? 0);

        if (in_array($lang, self::SUPPORTED, true) && $userId > 0) {
            AdminUserModel::setLang($userId, $lang);
            $_SESSION['admin_lang'] = $lang;
        }

        $referer = $request->getServerParams()['HTTP_REFERER'] ?? '/admin';
        return $this->redirect($response, $referer);
    }
}
```

- [ ] **Step 2: Register the route in routes.php**

Add the `use` import for `AdminLangController` alongside the other admin controller imports in `src/routes.php`:

```php
use App\Controllers\Admin\AdminLangController;
```

Then add the route inside the admin group, after the Settings routes (before the Auto-translate closure):

```php
    // Language switcher
    $group->get('/set-lang', AdminLangController::class . ':setLang');
```

- [ ] **Step 3: Run full test suite**

```bash
php vendor/bin/phpunit --testdox
```

Expected: all tests pass.

- [ ] **Step 4: Commit**

```bash
git add src/Controllers/Admin/AdminLangController.php src/routes.php
git commit -m "feat: add AdminLangController and GET /admin/set-lang route"
```

---

## Task 6: AuthController Patch

**Files:**
- Modify: `src/Controllers/Admin/AuthController.php`

**Interfaces:**
- Consumes: `AdminUserModel::getLang()` (Task 1)
- Produces: `$_SESSION['admin_lang']` written on every successful login and setup

- [ ] **Step 1: Patch loginSubmit() to write admin_lang**

In `src/Controllers/Admin/AuthController.php`, replace the `loginSubmit()` method:

```php
    public function loginSubmit(Request $request, Response $response, array $args): Response
    {
        if (session_status() === PHP_SESSION_NONE) session_start();
        $body     = (array) $request->getParsedBody();
        $email    = trim($body['email'] ?? '');
        $password = $body['password'] ?? '';

        $user = AdminUserModel::findByEmail($email);
        if ($user && password_verify($password, $user['password_hash'])) {
            $_SESSION['admin_user'] = ['id' => $user['id'], 'email' => $user['email'], 'role' => $user['role']];
            $_SESSION['admin_lang'] = AdminUserModel::getLang((int) $user['id']);
            return $this->redirect($response, '/admin');
        }

        return $this->renderAdmin($request, $response, 'admin/login.twig', ['error' => 'Nesprávný e-mail nebo heslo.']);
    }
```

- [ ] **Step 2: Patch setupSubmit() to write admin_lang**

Replace the `setupSubmit()` method (new user always gets the default 'cs'):

```php
    public function setupSubmit(Request $request, Response $response, array $args): Response
    {
        if (AdminUserModel::count() > 0) {
            return $this->redirect($response, '/admin/login');
        }
        $body  = (array) $request->getParsedBody();
        $email = trim($body['email'] ?? '');
        $pass  = $body['password'] ?? '';

        if (!$email || strlen($pass) < 8) {
            return $this->renderAdmin($request, $response, 'admin/setup.twig', ['error' => 'Vyplňte e-mail a heslo (min. 8 znaků).']);
        }

        $hash = password_hash($pass, PASSWORD_BCRYPT);
        AdminUserModel::create($email, $hash, 'admin');

        if (session_status() === PHP_SESSION_NONE) session_start();
        $user = AdminUserModel::findByEmail($email);
        $_SESSION['admin_user'] = ['id' => $user['id'], 'email' => $user['email'], 'role' => $user['role']];
        $_SESSION['admin_lang'] = 'cs';
        return $this->redirect($response, '/admin');
    }
```

- [ ] **Step 3: Run full test suite**

```bash
php vendor/bin/phpunit --testdox
```

Expected: all tests pass.

- [ ] **Step 4: Commit**

```bash
git add src/Controllers/Admin/AuthController.php
git commit -m "feat: write admin_lang to session on login and setup"
```

---

## Task 7: Templates + CSS

**Files:**
- Modify: `templates/layout/admin-base.twig`
- Modify: `templates/admin/settings/index.twig`
- Modify: `www/assets/css/admin.css`

**Interfaces:**
- Consumes: `t()` Twig function (from Task 4), `admin_lang` template variable (from Task 4)
- Produces: translated nav, lang switcher UI, translated Settings page

- [ ] **Step 1: Replace admin-base.twig**

Full content of `templates/layout/admin-base.twig`:
```twig
<!DOCTYPE html>
<html lang="cs">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Admin{% endblock %} — BalonkyDecor</title>
    <link rel="stylesheet" href="/assets/css/style.css">
    <link rel="stylesheet" href="/assets/css/admin.css">
    {% block head %}{% endblock %}
</head>
<body class="admin-body">
    <aside class="admin-sidebar">
        <div class="admin-logo">BalonkyDecor</div>
        <nav class="admin-nav">
            <a href="/admin">{{ t('nav.dashboard') }}</a>
            <a href="/admin/products">{{ t('nav.products') }}</a>
            <a href="/admin/categories">{{ t('nav.categories') }}</a>
            <a href="/admin/orders">{{ t('nav.orders') }}</a>
            <a href="/admin/gallery">{{ t('nav.gallery') }}</a>
            <a href="/admin/blog">{{ t('nav.blog') }}</a>
            <a href="/admin/pages">{{ t('nav.pages') }}</a>
            <a href="/admin/settings">{{ t('nav.settings') }}</a>
            {% if session_role == 'admin' %}<a href="/admin/users">{{ t('nav.users') }}</a>{% endif %}
        </nav>
        <div style="margin-top:auto;padding:1rem;border-top:1px solid rgba(255,255,255,0.1);">
            <div style="font-size:0.8rem;color:#a0a0b8;margin-bottom:0.25rem;">{{ session_email }}</div>
            <div style="font-size:0.75rem;color:#6060a0;text-transform:uppercase;letter-spacing:0.05em;margin-bottom:0.75rem;">{{ session_role }}</div>
            <div class="admin-lang-switcher">
                {% for code, label in {cs: 'CZ', sk: 'SK', en: 'EN', uk: 'UA', ru: 'RU'} %}
                    <a href="/admin/set-lang?l={{ code }}"{% if admin_lang == code %} class="active"{% endif %}>{{ label }}</a>
                {% endfor %}
            </div>
            <a href="/admin/logout" style="color:#c0c0d0;font-size:0.85rem;">{{ t('nav.logout') }}</a>
        </div>
    </aside>
    <main class="admin-main">
        {% if flash %}
            <div class="flash-{{ flash.type }}">{{ flash.message }}</div>
        {% endif %}
        {% block content %}{% endblock %}
    </main>
    {% block scripts %}{% endblock %}
</body>
</html>
```

- [ ] **Step 2: Replace settings/index.twig**

Full content of `templates/admin/settings/index.twig`:
```twig
{% extends "layout/admin-base.twig" %}
{% block title %}{{ t('settings.title') }}{% endblock %}
{% block content %}
<div class="admin-topbar"><h1>{{ t('settings.title') }}</h1></div>
<form method="POST" action="/admin/settings" class="admin-form">
    <h3>{{ t('settings.web') }}</h3>
    <div class="form-group">
        <label>{{ t('settings.site_name') }}</label>
        <input type="text" name="site_name" value="{{ settings.site_name ?? '' }}">
    </div>
    <div class="form-group">
        <label>{{ t('settings.contact_email') }}</label>
        <input type="email" name="contact_email" value="{{ settings.contact_email ?? '' }}">
    </div>
    <div class="form-group">
        <label>{{ t('settings.phone') }}</label>
        <input type="text" name="contact_phone" value="{{ settings.contact_phone ?? '' }}">
    </div>

    <h3>{{ t('settings.smtp') }}</h3>
    <div class="form-group">
        <label>{{ t('settings.smtp_host') }}</label>
        <input type="text" name="smtp_host" value="{{ settings.smtp_host ?? '' }}">
    </div>
    <div class="form-group">
        <label>{{ t('settings.smtp_port') }}</label>
        <input type="number" name="smtp_port" value="{{ settings.smtp_port ?? '587' }}">
    </div>
    <div class="form-group">
        <label>{{ t('settings.smtp_user') }}</label>
        <input type="text" name="smtp_user" value="{{ settings.smtp_user ?? '' }}">
    </div>
    <div class="form-group">
        <label>{{ t('settings.smtp_pass') }}</label>
        <input type="password" name="smtp_pass" value="{{ settings.smtp_pass ?? '' }}" autocomplete="new-password">
    </div>
    <div class="form-group">
        <label>{{ t('settings.smtp_from') }}</label>
        <input type="email" name="smtp_from" value="{{ settings.smtp_from ?? '' }}">
    </div>

    <h3>{{ t('settings.gopay') }}</h3>
    <p style="color:#666;font-size:0.9rem;">{{ t('settings.gopay_hint') }}</p>
    <div class="form-group">
        <label>{{ t('settings.gopay_go_id') }}</label>
        <input type="text" name="gopay_go_id" value="{{ settings.gopay_go_id ?? '' }}">
    </div>
    <div class="form-group">
        <label>{{ t('settings.client_id') }}</label>
        <input type="text" name="gopay_client_id" value="{{ settings.gopay_client_id ?? '' }}">
    </div>
    <div class="form-group">
        <label>{{ t('settings.client_secret') }}</label>
        <input type="password" name="gopay_client_secret" value="{{ settings.gopay_client_secret ?? '' }}" autocomplete="new-password">
    </div>
    <div class="form-group">
        <label>
            <input type="checkbox" name="gopay_test_mode" value="1" {% if settings.gopay_test_mode != '0' %}checked{% endif %}>
            {{ t('settings.test_mode') }}
        </label>
    </div>

    <div class="form-actions">
        <button type="submit" class="btn btn-primary">{{ t('settings.save') }}</button>
    </div>
</form>
{% endblock %}
```

- [ ] **Step 3: Add lang switcher CSS to admin.css**

Append to the end of `www/assets/css/admin.css`:
```css
.admin-lang-switcher { display: flex; gap: 0.4rem; margin-bottom: 0.75rem; font-size: 0.75rem; flex-wrap: wrap; }
.admin-lang-switcher a { color: #6060a0; text-decoration: none; }
.admin-lang-switcher a:hover { color: #a0a0c8; }
.admin-lang-switcher a.active { color: #ffffff; font-weight: 600; }
```

- [ ] **Step 4: Smoke-test in the browser**

```bash
php -S localhost:8080 -t www
```

1. Navigate to `http://localhost:8080/admin` — log in.
2. Verify nav links show Czech labels (Dashboard, Produkty, Kategorie …).
3. Verify the lang switcher row shows `CZ · SK · EN · UA · RU` with CZ highlighted.
4. Click **EN** — page reloads, nav shows English labels (Dashboard, Products, Categories …).
5. Navigate to `/admin/settings` — verify all labels are in English.
6. Click **UA** — nav and settings labels switch to Ukrainian.
7. Log out and log back in — verify the language preference is remembered (loaded from DB).

- [ ] **Step 5: Run full test suite one last time**

```bash
php vendor/bin/phpunit --testdox
```

Expected: all tests pass (37 existing + 6 new = 43 tests).

- [ ] **Step 6: Commit**

```bash
git add templates/layout/admin-base.twig templates/admin/settings/index.twig www/assets/css/admin.css
git commit -m "feat: translate admin nav and settings page, add lang switcher"
```

---

## Iteration 2 — Dashboard, Users, Blog

> Tasks 1–7 above are complete. Tasks 8–10 extend the same pattern to three more sections.
> After Task 10 each lang file will have 83 keys (28 existing + 55 new).

---

## Task 8: Dashboard i18n

**Files:**
- Modify: `lang/admin/cs.json`
- Modify: `lang/admin/en.json`
- Modify: `lang/admin/ru.json`
- Modify: `lang/admin/sk.json`
- Modify: `lang/admin/uk.json`
- Modify: `templates/admin/dashboard.twig`

**Interfaces:**
- Consumes: existing `t()` wiring from Task 4
- Produces: 12 `dashboard.*` translation keys; `dashboard.twig` uses `t()` for all strings

- [ ] **Step 1: Add `dashboard.*` keys to all 5 lang files**

In each file, find the last line `"settings.save": "..."` and add a comma after it. Then insert the following block before the closing `}`.

**cs.json — add after `settings.save`:**
```json
  "dashboard.title": "Dashboard",
  "dashboard.stats.orders_today": "Objednávky dnes",
  "dashboard.stats.orders_pending": "Čekající objednávky",
  "dashboard.stats.orders_total": "Celkem objednávek",
  "dashboard.stats.products_active": "Aktivní produkty",
  "dashboard.recent_orders": "Poslední objednávky",
  "dashboard.col.number": "Číslo",
  "dashboard.col.customer": "Zákazník",
  "dashboard.col.total": "Celkem",
  "dashboard.col.status": "Status",
  "dashboard.col.created": "Vytvořena",
  "dashboard.no_orders": "Žádné objednávky."
```

**en.json — add after `settings.save`:**
```json
  "dashboard.title": "Dashboard",
  "dashboard.stats.orders_today": "Orders today",
  "dashboard.stats.orders_pending": "Pending orders",
  "dashboard.stats.orders_total": "Total orders",
  "dashboard.stats.products_active": "Active products",
  "dashboard.recent_orders": "Recent orders",
  "dashboard.col.number": "Number",
  "dashboard.col.customer": "Customer",
  "dashboard.col.total": "Total",
  "dashboard.col.status": "Status",
  "dashboard.col.created": "Created",
  "dashboard.no_orders": "No orders."
```

**ru.json — add after `settings.save`:**
```json
  "dashboard.title": "Дашборд",
  "dashboard.stats.orders_today": "Заказы сегодня",
  "dashboard.stats.orders_pending": "Ожидающие заказы",
  "dashboard.stats.orders_total": "Всего заказов",
  "dashboard.stats.products_active": "Активные товары",
  "dashboard.recent_orders": "Последние заказы",
  "dashboard.col.number": "Номер",
  "dashboard.col.customer": "Клиент",
  "dashboard.col.total": "Итого",
  "dashboard.col.status": "Статус",
  "dashboard.col.created": "Создан",
  "dashboard.no_orders": "Нет заказов."
```

**sk.json — add after `settings.save`:**
```json
  "dashboard.title": "Dashboard",
  "dashboard.stats.orders_today": "Objednávky dnes",
  "dashboard.stats.orders_pending": "Čakajúce objednávky",
  "dashboard.stats.orders_total": "Celkom objednávok",
  "dashboard.stats.products_active": "Aktívne produkty",
  "dashboard.recent_orders": "Posledné objednávky",
  "dashboard.col.number": "Číslo",
  "dashboard.col.customer": "Zákazník",
  "dashboard.col.total": "Celkom",
  "dashboard.col.status": "Stav",
  "dashboard.col.created": "Vytvorená",
  "dashboard.no_orders": "Žiadne objednávky."
```

**uk.json — add after `settings.save`:**
```json
  "dashboard.title": "Дашборд",
  "dashboard.stats.orders_today": "Замовлення сьогодні",
  "dashboard.stats.orders_pending": "Очікуючі замовлення",
  "dashboard.stats.orders_total": "Всього замовлень",
  "dashboard.stats.products_active": "Активні товари",
  "dashboard.recent_orders": "Останні замовлення",
  "dashboard.col.number": "Номер",
  "dashboard.col.customer": "Клієнт",
  "dashboard.col.total": "Разом",
  "dashboard.col.status": "Статус",
  "dashboard.col.created": "Створено",
  "dashboard.no_orders": "Немає замовлень."
```

- [ ] **Step 2: Verify all files have 40 keys and valid JSON**

```bash
for f in lang/admin/*.json; do echo -n "$f: "; php -r "echo count(json_decode(file_get_contents('$f'), true)) . ' keys\n';"; done
```

Expected:
```
lang/admin/cs.json: 40 keys
lang/admin/en.json: 40 keys
lang/admin/ru.json: 40 keys
lang/admin/sk.json: 40 keys
lang/admin/uk.json: 40 keys
```

- [ ] **Step 3: Replace templates/admin/dashboard.twig**

Full new content:
```twig
{% extends "layout/admin-base.twig" %}
{% block title %}{{ t('dashboard.title') }}{% endblock %}
{% block content %}
<div class="admin-topbar"><h1>{{ t('dashboard.title') }}</h1></div>
<div class="stat-grid">
    <div class="stat-card">
        <div class="stat-value">{{ stats.orders_today }}</div>
        <div class="stat-label">{{ t('dashboard.stats.orders_today') }}</div>
    </div>
    <div class="stat-card">
        <div class="stat-value">{{ stats.orders_pending }}</div>
        <div class="stat-label">{{ t('dashboard.stats.orders_pending') }}</div>
    </div>
    <div class="stat-card">
        <div class="stat-value">{{ stats.orders_total }}</div>
        <div class="stat-label">{{ t('dashboard.stats.orders_total') }}</div>
    </div>
    <div class="stat-card">
        <div class="stat-value">{{ stats.products_active }}</div>
        <div class="stat-label">{{ t('dashboard.stats.products_active') }}</div>
    </div>
</div>
<h2>{{ t('dashboard.recent_orders') }}</h2>
<table class="admin-table">
    <thead>
        <tr>
            <th>{{ t('dashboard.col.number') }}</th>
            <th>{{ t('dashboard.col.customer') }}</th>
            <th>{{ t('dashboard.col.total') }}</th>
            <th>{{ t('dashboard.col.status') }}</th>
            <th>{{ t('dashboard.col.created') }}</th>
        </tr>
    </thead>
    <tbody>
    {% for o in recent %}
    <tr>
        <td><a href="/admin/orders/{{ o.order_number }}">{{ o.order_number }}</a></td>
        <td>{{ o.customer_name }}</td>
        <td>{{ o.total_amount|number_format(2, '.', ' ') }} Kč</td>
        <td><span class="badge badge-{{ o.status }}">{{ o.status }}</span></td>
        <td>{{ o.created_at }}</td>
    </tr>
    {% else %}
    <tr><td colspan="5">{{ t('dashboard.no_orders') }}</td></tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %}
```

- [ ] **Step 4: Smoke-test in the browser**

Start the server if not already running:
```bash
php -S localhost:8080 -t www
```

1. Log in at `http://localhost:8080/admin`.
2. Visit `/admin` — verify Czech strings: "Objednávky dnes", "Čekající objednávky", etc.
3. Switch to **EN** via the lang switcher.
4. Visit `/admin` again — verify English strings: "Orders today", "Pending orders", "Recent orders", column headers "Number", "Customer", "Total", "Created".
5. Verify the "no orders" row renders correctly if there are no recent orders.

- [ ] **Step 5: Commit**

```bash
git add lang/admin/cs.json lang/admin/en.json lang/admin/ru.json lang/admin/sk.json lang/admin/uk.json templates/admin/dashboard.twig
git commit -m "feat: translate admin dashboard"
```

---

## Task 9: Users i18n

**Files:**
- Modify: `lang/admin/cs.json`
- Modify: `lang/admin/en.json`
- Modify: `lang/admin/ru.json`
- Modify: `lang/admin/sk.json`
- Modify: `lang/admin/uk.json`
- Modify: `templates/admin/users/index.twig`
- Modify: `templates/admin/users/form.twig`

**Interfaces:**
- Consumes: `t()` Twig function
- Produces: 19 `users.*` translation keys; both user templates use `t()` for all strings

- [ ] **Step 1: Add `users.*` keys to all 5 lang files**

Add after the last `dashboard.*` key in each file (add a comma after `"dashboard.no_orders": "..."` first).

**cs.json:**
```json
  "users.title": "Uživatelé",
  "users.add": "+ Přidat uživatele",
  "users.col.id": "ID",
  "users.col.email": "E-mail",
  "users.col.role": "Role",
  "users.col.created": "Vytvořen",
  "users.col.actions": "Akce",
  "users.new_password": "Nové heslo",
  "users.change_password": "Změnit heslo",
  "users.delete": "Smazat",
  "users.confirm_delete": "Smazat uživatele?",
  "users.no_users": "Žádní uživatelé.",
  "users.form.title_new": "Nový uživatel",
  "users.form.back": "← Zpět",
  "users.form.email": "E-mail",
  "users.form.password": "Heslo (min. 8 znaků)",
  "users.form.role": "Role",
  "users.form.create": "Vytvořit",
  "users.form.cancel": "Zrušit"
```

**en.json:**
```json
  "users.title": "Users",
  "users.add": "+ Add user",
  "users.col.id": "ID",
  "users.col.email": "E-mail",
  "users.col.role": "Role",
  "users.col.created": "Created",
  "users.col.actions": "Actions",
  "users.new_password": "New password",
  "users.change_password": "Change password",
  "users.delete": "Delete",
  "users.confirm_delete": "Delete user?",
  "users.no_users": "No users.",
  "users.form.title_new": "New user",
  "users.form.back": "← Back",
  "users.form.email": "E-mail",
  "users.form.password": "Password (min. 8 characters)",
  "users.form.role": "Role",
  "users.form.create": "Create",
  "users.form.cancel": "Cancel"
```

**ru.json:**
```json
  "users.title": "Пользователи",
  "users.add": "+ Добавить пользователя",
  "users.col.id": "ID",
  "users.col.email": "E-mail",
  "users.col.role": "Роль",
  "users.col.created": "Создан",
  "users.col.actions": "Действия",
  "users.new_password": "Новый пароль",
  "users.change_password": "Сменить пароль",
  "users.delete": "Удалить",
  "users.confirm_delete": "Удалить пользователя?",
  "users.no_users": "Нет пользователей.",
  "users.form.title_new": "Новый пользователь",
  "users.form.back": "← Назад",
  "users.form.email": "E-mail",
  "users.form.password": "Пароль (мин. 8 символов)",
  "users.form.role": "Роль",
  "users.form.create": "Создать",
  "users.form.cancel": "Отмена"
```

**sk.json:**
```json
  "users.title": "Používatelia",
  "users.add": "+ Pridať používateľa",
  "users.col.id": "ID",
  "users.col.email": "E-mail",
  "users.col.role": "Rola",
  "users.col.created": "Vytvorený",
  "users.col.actions": "Akcie",
  "users.new_password": "Nové heslo",
  "users.change_password": "Zmeniť heslo",
  "users.delete": "Zmazať",
  "users.confirm_delete": "Zmazať používateľa?",
  "users.no_users": "Žiadni používatelia.",
  "users.form.title_new": "Nový používateľ",
  "users.form.back": "← Späť",
  "users.form.email": "E-mail",
  "users.form.password": "Heslo (min. 8 znakov)",
  "users.form.role": "Rola",
  "users.form.create": "Vytvoriť",
  "users.form.cancel": "Zrušiť"
```

**uk.json:**
```json
  "users.title": "Користувачі",
  "users.add": "+ Додати користувача",
  "users.col.id": "ID",
  "users.col.email": "E-mail",
  "users.col.role": "Роль",
  "users.col.created": "Створено",
  "users.col.actions": "Дії",
  "users.new_password": "Новий пароль",
  "users.change_password": "Змінити пароль",
  "users.delete": "Видалити",
  "users.confirm_delete": "Видалити користувача?",
  "users.no_users": "Немає користувачів.",
  "users.form.title_new": "Новий користувач",
  "users.form.back": "← Назад",
  "users.form.email": "E-mail",
  "users.form.password": "Пароль (мін. 8 символів)",
  "users.form.role": "Роль",
  "users.form.create": "Створити",
  "users.form.cancel": "Скасувати"
```

- [ ] **Step 2: Verify all files have 59 keys and valid JSON**

```bash
for f in lang/admin/*.json; do echo -n "$f: "; php -r "echo count(json_decode(file_get_contents('$f'), true)) . ' keys\n';"; done
```

Expected:
```
lang/admin/cs.json: 59 keys
lang/admin/en.json: 59 keys
lang/admin/ru.json: 59 keys
lang/admin/sk.json: 59 keys
lang/admin/uk.json: 59 keys
```

- [ ] **Step 3: Replace templates/admin/users/index.twig**

Full new content:
```twig
{% extends "layout/admin-base.twig" %}
{% block title %}{{ t('users.title') }}{% endblock %}
{% block content %}
<div class="admin-topbar">
    <h1>{{ t('users.title') }}</h1>
    <a href="/admin/users/new" class="btn btn-primary">{{ t('users.add') }}</a>
</div>
<table class="admin-table">
    <thead>
        <tr>
            <th>{{ t('users.col.id') }}</th>
            <th>{{ t('users.col.email') }}</th>
            <th>{{ t('users.col.role') }}</th>
            <th>{{ t('users.col.created') }}</th>
            <th>{{ t('users.col.actions') }}</th>
        </tr>
    </thead>
    <tbody>
    {% for u in users %}
    <tr>
        <td>{{ u.id }}</td>
        <td>{{ u.email }}</td>
        <td>{{ u.role }}</td>
        <td>{{ u.created_at }}</td>
        <td>
            <form method="POST" action="/admin/users/{{ u.id }}/password" style="display:inline-flex;gap:0.4rem;align-items:center;">
                <input type="password" name="password" placeholder="{{ t('users.new_password') }}" minlength="8" style="width:140px;">
                <button type="submit" class="btn btn-secondary" style="padding:0.3rem 0.6rem;font-size:0.85rem;">{{ t('users.change_password') }}</button>
            </form>
            |
            <form method="POST" action="/admin/users/{{ u.id }}/delete" style="display:inline" onsubmit="return confirm('Smazat uživatele?')">
                <button class="btn-link">{{ t('users.delete') }}</button>
            </form>
        </td>
    </tr>
    {% else %}
    <tr><td colspan="5">{{ t('users.no_users') }}</td></tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %}
```

Note: `onsubmit="return confirm('Smazat uživatele?')"` remains hardcoded Czech per spec (same pattern as controller flash messages).

- [ ] **Step 4: Replace templates/admin/users/form.twig**

Full new content:
```twig
{% extends "layout/admin-base.twig" %}
{% block title %}{{ t('users.form.title_new') }}{% endblock %}
{% block content %}
<div class="admin-topbar">
    <h1>{{ t('users.form.title_new') }}</h1>
    <a href="/admin/users" class="btn btn-secondary">{{ t('users.form.back') }}</a>
</div>
<form method="POST" action="/admin/users/new" class="admin-form">
    <div class="form-group">
        <label>{{ t('users.form.email') }}</label>
        <input type="email" name="email" required autofocus>
    </div>
    <div class="form-group">
        <label>{{ t('users.form.password') }}</label>
        <input type="password" name="password" required minlength="8">
    </div>
    <div class="form-group">
        <label>{{ t('users.form.role') }}</label>
        <select name="role">
            <option value="admin">Admin</option>
            <option value="editor">Editor</option>
        </select>
    </div>
    <div class="form-actions">
        <button type="submit" class="btn btn-primary">{{ t('users.form.create') }}</button>
        <a href="/admin/users" class="btn btn-secondary">{{ t('users.form.cancel') }}</a>
    </div>
</form>
{% endblock %}
```

- [ ] **Step 5: Smoke-test in the browser**

1. Switch to **EN** via the lang switcher.
2. Visit `/admin/users` — verify: "Users" heading, "+ Add user" button, column headers "ID", "E-mail", "Role", "Created", "Actions", "New password" placeholder, "Change password" button, "Delete" link, "No users." empty state.
3. Visit `/admin/users/new` — verify: "New user" heading, "← Back" button, "E-mail", "Password (min. 8 characters)", "Role", "Create" and "Cancel" buttons.

- [ ] **Step 6: Commit**

```bash
git add lang/admin/cs.json lang/admin/en.json lang/admin/ru.json lang/admin/sk.json lang/admin/uk.json templates/admin/users/index.twig templates/admin/users/form.twig
git commit -m "feat: translate admin users section"
```

---

## Task 10: Blog i18n

**Files:**
- Modify: `lang/admin/cs.json`
- Modify: `lang/admin/en.json`
- Modify: `lang/admin/ru.json`
- Modify: `lang/admin/sk.json`
- Modify: `lang/admin/uk.json`
- Modify: `templates/admin/blog/index.twig`
- Modify: `templates/admin/blog/form.twig`

**Interfaces:**
- Consumes: `t()` Twig function
- Produces: 24 `blog.*` translation keys; both blog templates use `t()` for all strings

- [ ] **Step 1: Add `blog.*` keys to all 5 lang files**

Add after the last `users.*` key in each file (add a comma after `"users.form.cancel": "..."` first).

**cs.json:**
```json
  "blog.title": "Blog",
  "blog.add": "+ Nový příspěvek",
  "blog.col.id": "ID",
  "blog.col.slug": "Slug",
  "blog.col.status": "Status",
  "blog.col.date": "Datum",
  "blog.col.actions": "Akce",
  "blog.edit": "Upravit",
  "blog.delete": "Smazat",
  "blog.confirm_delete": "Smazat příspěvek?",
  "blog.no_posts": "Žádné příspěvky.",
  "blog.form.title_new": "Nový příspěvek",
  "blog.form.title_edit": "Upravit příspěvek",
  "blog.form.back": "← Zpět",
  "blog.form.slug": "Slug",
  "blog.form.status": "Status",
  "blog.form.status_draft": "Koncept (draft)",
  "blog.form.status_published": "Publikováno",
  "blog.form.published_at": "Datum publikace",
  "blog.form.translations": "Překlady",
  "blog.form.title_label": "Nadpis",
  "blog.form.body_label": "Obsah — HTML povoleno",
  "blog.form.save": "Uložit",
  "blog.form.cancel": "Zrušit"
```

**en.json:**
```json
  "blog.title": "Blog",
  "blog.add": "+ New post",
  "blog.col.id": "ID",
  "blog.col.slug": "Slug",
  "blog.col.status": "Status",
  "blog.col.date": "Date",
  "blog.col.actions": "Actions",
  "blog.edit": "Edit",
  "blog.delete": "Delete",
  "blog.confirm_delete": "Delete post?",
  "blog.no_posts": "No posts.",
  "blog.form.title_new": "New post",
  "blog.form.title_edit": "Edit post",
  "blog.form.back": "← Back",
  "blog.form.slug": "Slug",
  "blog.form.status": "Status",
  "blog.form.status_draft": "Draft",
  "blog.form.status_published": "Published",
  "blog.form.published_at": "Publication date",
  "blog.form.translations": "Translations",
  "blog.form.title_label": "Title",
  "blog.form.body_label": "Content — HTML allowed",
  "blog.form.save": "Save",
  "blog.form.cancel": "Cancel"
```

**ru.json:**
```json
  "blog.title": "Блог",
  "blog.add": "+ Новая запись",
  "blog.col.id": "ID",
  "blog.col.slug": "Slug",
  "blog.col.status": "Статус",
  "blog.col.date": "Дата",
  "blog.col.actions": "Действия",
  "blog.edit": "Изменить",
  "blog.delete": "Удалить",
  "blog.confirm_delete": "Удалить запись?",
  "blog.no_posts": "Нет записей.",
  "blog.form.title_new": "Новая запись",
  "blog.form.title_edit": "Изменить запись",
  "blog.form.back": "← Назад",
  "blog.form.slug": "Slug",
  "blog.form.status": "Статус",
  "blog.form.status_draft": "Черновик",
  "blog.form.status_published": "Опубликовано",
  "blog.form.published_at": "Дата публикации",
  "blog.form.translations": "Переводы",
  "blog.form.title_label": "Заголовок",
  "blog.form.body_label": "Содержание — разрешён HTML",
  "blog.form.save": "Сохранить",
  "blog.form.cancel": "Отмена"
```

**sk.json:**
```json
  "blog.title": "Blog",
  "blog.add": "+ Nový príspevok",
  "blog.col.id": "ID",
  "blog.col.slug": "Slug",
  "blog.col.status": "Stav",
  "blog.col.date": "Dátum",
  "blog.col.actions": "Akcie",
  "blog.edit": "Upraviť",
  "blog.delete": "Zmazať",
  "blog.confirm_delete": "Zmazať príspevok?",
  "blog.no_posts": "Žiadne príspevky.",
  "blog.form.title_new": "Nový príspevok",
  "blog.form.title_edit": "Upraviť príspevok",
  "blog.form.back": "← Späť",
  "blog.form.slug": "Slug",
  "blog.form.status": "Stav",
  "blog.form.status_draft": "Koncept (draft)",
  "blog.form.status_published": "Publikované",
  "blog.form.published_at": "Dátum publikácie",
  "blog.form.translations": "Preklady",
  "blog.form.title_label": "Nadpis",
  "blog.form.body_label": "Obsah — HTML povolený",
  "blog.form.save": "Uložiť",
  "blog.form.cancel": "Zrušiť"
```

**uk.json:**
```json
  "blog.title": "Блог",
  "blog.add": "+ Новий пост",
  "blog.col.id": "ID",
  "blog.col.slug": "Slug",
  "blog.col.status": "Статус",
  "blog.col.date": "Дата",
  "blog.col.actions": "Дії",
  "blog.edit": "Редагувати",
  "blog.delete": "Видалити",
  "blog.confirm_delete": "Видалити пост?",
  "blog.no_posts": "Немає постів.",
  "blog.form.title_new": "Новий пост",
  "blog.form.title_edit": "Редагувати пост",
  "blog.form.back": "← Назад",
  "blog.form.slug": "Slug",
  "blog.form.status": "Статус",
  "blog.form.status_draft": "Чернетка",
  "blog.form.status_published": "Опубліковано",
  "blog.form.published_at": "Дата публікації",
  "blog.form.translations": "Переклади",
  "blog.form.title_label": "Заголовок",
  "blog.form.body_label": "Вміст — HTML дозволено",
  "blog.form.save": "Зберегти",
  "blog.form.cancel": "Скасувати"
```

- [ ] **Step 2: Verify all files have 83 keys and valid JSON**

```bash
for f in lang/admin/*.json; do echo -n "$f: "; php -r "echo count(json_decode(file_get_contents('$f'), true)) . ' keys\n';"; done
```

Expected:
```
lang/admin/cs.json: 83 keys
lang/admin/en.json: 83 keys
lang/admin/ru.json: 83 keys
lang/admin/sk.json: 83 keys
lang/admin/uk.json: 83 keys
```

- [ ] **Step 3: Replace templates/admin/blog/index.twig**

Full new content:
```twig
{% extends "layout/admin-base.twig" %}
{% block title %}{{ t('blog.title') }}{% endblock %}
{% block content %}
<div class="admin-topbar">
    <h1>{{ t('blog.title') }}</h1>
    <a href="/admin/blog/new" class="btn btn-primary">{{ t('blog.add') }}</a>
</div>
<table class="admin-table">
    <thead>
        <tr>
            <th>{{ t('blog.col.id') }}</th>
            <th>{{ t('blog.col.slug') }}</th>
            <th>{{ t('blog.col.status') }}</th>
            <th>{{ t('blog.col.date') }}</th>
            <th>{{ t('blog.col.actions') }}</th>
        </tr>
    </thead>
    <tbody>
    {% for p in posts %}
    <tr>
        <td>{{ p.id }}</td>
        <td>{{ p.slug }}</td>
        <td><span class="badge badge-{{ p.status == 'published' ? 'paid' : 'pending' }}">{{ p.status }}</span></td>
        <td>{{ p.published_at ?? '—' }}</td>
        <td>
            <a href="/admin/blog/{{ p.id }}/edit">{{ t('blog.edit') }}</a> |
            <form method="POST" action="/admin/blog/{{ p.id }}/delete" style="display:inline" onsubmit="return confirm('Smazat příspěvek?')">
                <button class="btn-link">{{ t('blog.delete') }}</button>
            </form>
        </td>
    </tr>
    {% else %}
    <tr><td colspan="5">{{ t('blog.no_posts') }}</td></tr>
    {% endfor %}
    </tbody>
</table>
{% if pages > 1 %}
<div class="pagination" style="margin-top:1rem;">
    {% for p in 1..pages %}
    <a href="?page={{ p }}" class="{% if p == page %}active{% endif %}">{{ p }}</a>
    {% endfor %}
</div>
{% endif %}
{% endblock %}
```

Note: `onsubmit="return confirm('Smazat příspěvek?')"` remains hardcoded Czech per spec.

- [ ] **Step 4: Replace templates/admin/blog/form.twig**

Full new content:
```twig
{% extends "layout/admin-base.twig" %}
{% block title %}{{ post ? t('blog.form.title_edit') : t('blog.form.title_new') }}{% endblock %}
{% block content %}
<div class="admin-topbar">
    <h1>{{ post ? t('blog.form.title_edit') : t('blog.form.title_new') }}</h1>
    <a href="/admin/blog" class="btn btn-secondary">{{ t('blog.form.back') }}</a>
</div>
<form method="POST" action="{{ post ? '/admin/blog/' ~ post.id ~ '/edit' : '/admin/blog/new' }}" class="admin-form">
    <div class="form-group">
        <label>{{ t('blog.form.slug') }}</label>
        <input type="text" name="slug" value="{{ post.slug ?? '' }}" required>
    </div>
    <div class="form-group">
        <label>{{ t('blog.form.status') }}</label>
        <select name="status">
            <option value="draft" {% if (post.status ?? 'draft') == 'draft' %}selected{% endif %}>{{ t('blog.form.status_draft') }}</option>
            <option value="published" {% if (post.status ?? '') == 'published' %}selected{% endif %}>{{ t('blog.form.status_published') }}</option>
        </select>
    </div>
    <div class="form-group">
        <label>{{ t('blog.form.published_at') }}</label>
        <input type="datetime-local" name="published_at" value="{{ post.published_at ?? '' }}">
    </div>
    <h3>{{ t('blog.form.translations') }}</h3>
    <div class="lang-tabs">
        {% for lang in langs %}
        <button type="button" class="lang-tab {% if loop.first %}active{% endif %}" data-lang="{{ lang }}">{{ lang|upper }}</button>
        {% endfor %}
    </div>
    {% for lang in langs %}
    <div class="lang-panel {% if loop.first %}active{% endif %}" id="panel-{{ lang }}">
        <div class="form-group">
            <label>{{ t('blog.form.title_label') }} ({{ lang|upper }})</label>
            <input type="text" name="t[{{ lang }}][title]" value="{{ translations[lang].title ?? '' }}">
        </div>
        <div class="form-group">
            <label>{{ t('blog.form.body_label') }} ({{ lang|upper }})</label>
            <textarea name="t[{{ lang }}][body]" style="min-height:300px;">{{ translations[lang].body ?? '' }}</textarea>
        </div>
    </div>
    {% endfor %}
    <div class="form-actions">
        <button type="submit" class="btn btn-primary">{{ t('blog.form.save') }}</button>
        <a href="/admin/blog" class="btn btn-secondary">{{ t('blog.form.cancel') }}</a>
    </div>
</form>
{% block scripts %}
<script>
document.querySelectorAll('.lang-tab').forEach(tab => {
    tab.addEventListener('click', () => {
        document.querySelectorAll('.lang-tab').forEach(t => t.classList.remove('active'));
        document.querySelectorAll('.lang-panel').forEach(p => p.classList.remove('active'));
        tab.classList.add('active');
        document.getElementById('panel-' + tab.dataset.lang).classList.add('active');
    });
});
</script>
{% endblock %}
{% endblock %}
```

- [ ] **Step 5: Smoke-test in the browser**

1. Switch to **EN** via the lang switcher.
2. Visit `/admin/blog` — verify: "Blog" heading, "+ New post" button, column headers "ID", "Slug", "Status", "Date", "Actions", "Edit" link, "Delete" button, "No posts." empty state.
3. Visit `/admin/blog/new` — verify: "New post" heading, "← Back" button, "Slug", "Status" label, "Draft"/"Published" select options, "Publication date", "Translations", "Title (CS)", "Content — HTML allowed (CS)" labels, "Save" and "Cancel" buttons.
4. Switch to **RU** and repeat — verify Cyrillic labels appear on both pages.

- [ ] **Step 6: Run full test suite**

```bash
php vendor/bin/phpunit --testdox
```

Expected: all existing tests pass (no new tests for template changes).

- [ ] **Step 7: Commit**

```bash
git add lang/admin/cs.json lang/admin/en.json lang/admin/ru.json lang/admin/sk.json lang/admin/uk.json templates/admin/blog/index.twig templates/admin/blog/form.twig
git commit -m "feat: translate admin blog section"
```

---

## Iteration 3 — Products, Categories

> Tasks 1–10 above are complete. Tasks 11–12 extend the same pattern to Products and Categories.
> After Task 12 each lang file will have 132 keys (83 existing + 49 new).

---

## Task 11: Products i18n

**Files:**
- Modify: `lang/admin/cs.json`
- Modify: `lang/admin/en.json`
- Modify: `lang/admin/ru.json`
- Modify: `lang/admin/sk.json`
- Modify: `lang/admin/uk.json`
- Modify: `templates/admin/products/index.twig`
- Modify: `templates/admin/products/form.twig`

**Interfaces:**
- Consumes: existing `t()` wiring (AdminLangMiddleware + AdminBaseController)
- Produces: 27 `products.*` translation keys; both product templates use `t()` for all visible strings

- [ ] **Step 1: Add `products.*` keys to all 5 lang files**

In each file, find the last line `"blog.form.cancel": "..."` and add a comma after it. Then insert the following block before the closing `}`.

**cs.json — add after `blog.form.cancel`:**
```json
  "products.title": "Produkty",
  "products.add": "+ Přidat produkt",
  "products.col.image": "Obrázek",
  "products.col.sku": "SKU",
  "products.col.category": "Kategorie",
  "products.col.price": "Cena",
  "products.col.active": "Aktivní",
  "products.col.actions": "Akce",
  "products.edit": "Upravit",
  "products.delete": "Smazat",
  "products.confirm_delete": "Smazat produkt?",
  "products.no_products": "Žádné produkty.",
  "products.form.title_new": "Nový produkt",
  "products.form.title_edit": "Upravit produkt",
  "products.form.back": "← Zpět",
  "products.form.sku": "SKU",
  "products.form.price": "Cena (Kč)",
  "products.form.category": "Kategorie",
  "products.form.active": "Aktivní",
  "products.form.add_image": "Přidat obrázek",
  "products.form.existing_images": "Stávající obrázky",
  "products.form.delete_image": "Smazat",
  "products.form.translations": "Překlady",
  "products.form.name_label": "Název",
  "products.form.desc_label": "Popis",
  "products.form.save": "Uložit",
  "products.form.cancel": "Zrušit"
```

**en.json — add after `blog.form.cancel`:**
```json
  "products.title": "Products",
  "products.add": "+ Add product",
  "products.col.image": "Image",
  "products.col.sku": "SKU",
  "products.col.category": "Category",
  "products.col.price": "Price",
  "products.col.active": "Active",
  "products.col.actions": "Actions",
  "products.edit": "Edit",
  "products.delete": "Delete",
  "products.confirm_delete": "Delete product?",
  "products.no_products": "No products.",
  "products.form.title_new": "New product",
  "products.form.title_edit": "Edit product",
  "products.form.back": "← Back",
  "products.form.sku": "SKU",
  "products.form.price": "Price (CZK)",
  "products.form.category": "Category",
  "products.form.active": "Active",
  "products.form.add_image": "Add image",
  "products.form.existing_images": "Existing images",
  "products.form.delete_image": "Delete",
  "products.form.translations": "Translations",
  "products.form.name_label": "Name",
  "products.form.desc_label": "Description",
  "products.form.save": "Save",
  "products.form.cancel": "Cancel"
```

**ru.json — add after `blog.form.cancel`:**
```json
  "products.title": "Товары",
  "products.add": "+ Добавить товар",
  "products.col.image": "Изображение",
  "products.col.sku": "SKU",
  "products.col.category": "Категория",
  "products.col.price": "Цена",
  "products.col.active": "Активный",
  "products.col.actions": "Действия",
  "products.edit": "Изменить",
  "products.delete": "Удалить",
  "products.confirm_delete": "Удалить товар?",
  "products.no_products": "Нет товаров.",
  "products.form.title_new": "Новый товар",
  "products.form.title_edit": "Изменить товар",
  "products.form.back": "← Назад",
  "products.form.sku": "SKU",
  "products.form.price": "Цена (Kč)",
  "products.form.category": "Категория",
  "products.form.active": "Активный",
  "products.form.add_image": "Добавить изображение",
  "products.form.existing_images": "Существующие изображения",
  "products.form.delete_image": "Удалить",
  "products.form.translations": "Переводы",
  "products.form.name_label": "Название",
  "products.form.desc_label": "Описание",
  "products.form.save": "Сохранить",
  "products.form.cancel": "Отмена"
```

**sk.json — add after `blog.form.cancel`:**
```json
  "products.title": "Produkty",
  "products.add": "+ Pridať produkt",
  "products.col.image": "Obrázok",
  "products.col.sku": "SKU",
  "products.col.category": "Kategória",
  "products.col.price": "Cena",
  "products.col.active": "Aktívny",
  "products.col.actions": "Akcie",
  "products.edit": "Upraviť",
  "products.delete": "Zmazať",
  "products.confirm_delete": "Zmazať produkt?",
  "products.no_products": "Žiadne produkty.",
  "products.form.title_new": "Nový produkt",
  "products.form.title_edit": "Upraviť produkt",
  "products.form.back": "← Späť",
  "products.form.sku": "SKU",
  "products.form.price": "Cena (Kč)",
  "products.form.category": "Kategória",
  "products.form.active": "Aktívny",
  "products.form.add_image": "Pridať obrázok",
  "products.form.existing_images": "Existujúce obrázky",
  "products.form.delete_image": "Zmazať",
  "products.form.translations": "Preklady",
  "products.form.name_label": "Názov",
  "products.form.desc_label": "Popis",
  "products.form.save": "Uložiť",
  "products.form.cancel": "Zrušiť"
```

**uk.json — add after `blog.form.cancel`:**
```json
  "products.title": "Товари",
  "products.add": "+ Додати товар",
  "products.col.image": "Зображення",
  "products.col.sku": "SKU",
  "products.col.category": "Категорія",
  "products.col.price": "Ціна",
  "products.col.active": "Активний",
  "products.col.actions": "Дії",
  "products.edit": "Редагувати",
  "products.delete": "Видалити",
  "products.confirm_delete": "Видалити товар?",
  "products.no_products": "Немає товарів.",
  "products.form.title_new": "Новий товар",
  "products.form.title_edit": "Редагувати товар",
  "products.form.back": "← Назад",
  "products.form.sku": "SKU",
  "products.form.price": "Ціна (Kč)",
  "products.form.category": "Категорія",
  "products.form.active": "Активний",
  "products.form.add_image": "Додати зображення",
  "products.form.existing_images": "Наявні зображення",
  "products.form.delete_image": "Видалити",
  "products.form.translations": "Переклади",
  "products.form.name_label": "Назва",
  "products.form.desc_label": "Опис",
  "products.form.save": "Зберегти",
  "products.form.cancel": "Скасувати"
```

- [ ] **Step 2: Verify all files have 110 keys and valid JSON**

```bash
for f in lang/admin/*.json; do echo -n "$f: "; php -r "echo count(json_decode(file_get_contents('$f'), true)) . ' keys\n';"; done
```

Expected:
```
lang/admin/cs.json: 110 keys
lang/admin/en.json: 110 keys
lang/admin/ru.json: 110 keys
lang/admin/sk.json: 110 keys
lang/admin/uk.json: 110 keys
```

- [ ] **Step 3: Replace templates/admin/products/index.twig**

Full new content:
```twig
{% extends "layout/admin-base.twig" %}
{% block title %}{{ t('products.title') }}{% endblock %}
{% block content %}
<div class="admin-topbar">
    <h1>{{ t('products.title') }}</h1>
    <a href="/admin/products/new" class="btn btn-primary">{{ t('products.add') }}</a>
</div>
<table class="admin-table">
    <thead>
        <tr>
            <th>{{ t('products.col.image') }}</th>
            <th>{{ t('products.col.sku') }}</th>
            <th>{{ t('products.col.category') }}</th>
            <th>{{ t('products.col.price') }}</th>
            <th>{{ t('products.col.active') }}</th>
            <th>{{ t('products.col.actions') }}</th>
        </tr>
    </thead>
    <tbody>
    {% for p in products %}
    <tr>
        <td>
            {% if p.primary_image %}
            <img src="/assets/uploads/products/thumb_{{ p.primary_image }}" class="img-thumb">
            {% endif %}
        </td>
        <td>{{ p.sku }}</td>
        <td>{{ p.category_name ?? '—' }}</td>
        <td>{{ p.price|number_format(2, '.', ' ') }} Kč</td>
        <td>{{ p.is_active ? '✓' : '—' }}</td>
        <td>
            <a href="/admin/products/{{ p.id }}/edit">{{ t('products.edit') }}</a> |
            <form method="POST" action="/admin/products/{{ p.id }}/delete" style="display:inline" onsubmit="return confirm('Smazat produkt?')">
                <button class="btn-link">{{ t('products.delete') }}</button>
            </form>
        </td>
    </tr>
    {% else %}
    <tr><td colspan="6">{{ t('products.no_products') }}</td></tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %}
```

Note: `onsubmit="return confirm('Smazat produkt?')"` remains hardcoded Czech per spec.

- [ ] **Step 4: Replace templates/admin/products/form.twig**

Full new content:
```twig
{% extends "layout/admin-base.twig" %}
{% block title %}{{ product ? t('products.form.title_edit') : t('products.form.title_new') }}{% endblock %}
{% block content %}
<div class="admin-topbar">
    <h1>{{ product ? t('products.form.title_edit') : t('products.form.title_new') }}</h1>
    <a href="/admin/products" class="btn btn-secondary">{{ t('products.form.back') }}</a>
</div>
<form method="POST" action="{{ product ? '/admin/products/' ~ product.id ~ '/edit' : '/admin/products/new' }}" enctype="multipart/form-data" class="admin-form">
    <div class="form-group">
        <label>{{ t('products.form.sku') }}</label>
        <input type="text" name="sku" value="{{ product.sku ?? '' }}" required>
    </div>
    <div class="form-group">
        <label>{{ t('products.form.price') }}</label>
        <input type="number" name="price" step="0.01" min="0" value="{{ product.price ?? '0.00' }}" required>
    </div>
    <div class="form-group">
        <label>{{ t('products.form.category') }}</label>
        <select name="category_id">
            {% for cat in categories %}
            <option value="{{ cat.id }}" {% if product.category_id == cat.id %}selected{% endif %}>{{ cat.name }}</option>
            {% endfor %}
        </select>
    </div>
    <div class="form-group">
        <label>
            <input type="checkbox" name="is_active" value="1" {% if product is null or product.is_active %}checked{% endif %}>
            {{ t('products.form.active') }}
        </label>
    </div>
    <div class="form-group">
        <label>{{ t('products.form.add_image') }}</label>
        <input type="file" name="image" accept="image/*">
    </div>
    {% if product and product.images %}
    <div class="form-group">
        <label>{{ t('products.form.existing_images') }}</label>
        <div style="display:flex;gap:0.5rem;flex-wrap:wrap;margin-top:0.5rem;">
        {% for img in product.images %}
        <div style="text-align:center;">
            <img src="/assets/uploads/products/thumb_{{ img.filename }}" class="img-thumb"><br>
            <form method="POST" action="/admin/products/{{ product.id }}/image/{{ img.id }}/delete">
                <button class="btn-link" style="font-size:0.8rem">{{ t('products.form.delete_image') }}</button>
            </form>
        </div>
        {% endfor %}
        </div>
    </div>
    {% endif %}
    <h3>{{ t('products.form.translations') }}</h3>
    <div class="lang-tabs">
        {% for lang in langs %}
        <button type="button" class="lang-tab {% if loop.first %}active{% endif %}" data-lang="{{ lang }}">{{ lang|upper }}</button>
        {% endfor %}
    </div>
    {% for lang in langs %}
    <div class="lang-panel {% if loop.first %}active{% endif %}" id="panel-{{ lang }}">
        <div class="form-group">
            <label>{{ t('products.form.name_label') }} ({{ lang|upper }})</label>
            <input type="text" name="t[{{ lang }}][name]" value="{{ translations[lang].name ?? '' }}">
        </div>
        <div class="form-group">
            <label>{{ t('products.form.desc_label') }} ({{ lang|upper }})</label>
            <textarea name="t[{{ lang }}][description]">{{ translations[lang].description ?? '' }}</textarea>
        </div>
    </div>
    {% endfor %}
    <div class="form-actions">
        <button type="submit" class="btn btn-primary">{{ t('products.form.save') }}</button>
        <a href="/admin/products" class="btn btn-secondary">{{ t('products.form.cancel') }}</a>
    </div>
</form>
{% block scripts %}
<script>
document.querySelectorAll('.lang-tab').forEach(tab => {
    tab.addEventListener('click', () => {
        document.querySelectorAll('.lang-tab').forEach(t => t.classList.remove('active'));
        document.querySelectorAll('.lang-panel').forEach(p => p.classList.remove('active'));
        tab.classList.add('active');
        document.getElementById('panel-' + tab.dataset.lang).classList.add('active');
    });
});
</script>
{% endblock %}
{% endblock %}
```

- [ ] **Step 5: Smoke-test in the browser**

Start server if not running:
```bash
php -S localhost:8080 -t www
```

1. Switch to **EN** via the lang switcher.
2. Visit `/admin/products` — verify: "Products" heading, "+ Add product" button, column headers "Image", "SKU", "Category", "Price", "Active", "Actions", "Edit" link, "Delete" button, "No products." empty state.
3. Visit `/admin/products/new` — verify: "New product" heading, "← Back", field labels "SKU", "Price (CZK)", "Category", "Active", "Add image", "Translations", "Name (CS)", "Description (CS)", "Save" and "Cancel" buttons.
4. Switch to **RU** — verify Cyrillic labels on both pages.

- [ ] **Step 6: Commit**

```bash
git add lang/admin/cs.json lang/admin/en.json lang/admin/ru.json lang/admin/sk.json lang/admin/uk.json templates/admin/products/index.twig templates/admin/products/form.twig
git commit -m "feat: translate admin products section"
```

---

## Task 12: Categories i18n

**Files:**
- Modify: `lang/admin/cs.json`
- Modify: `lang/admin/en.json`
- Modify: `lang/admin/ru.json`
- Modify: `lang/admin/sk.json`
- Modify: `lang/admin/uk.json`
- Modify: `templates/admin/categories/index.twig`
- Modify: `templates/admin/categories/form.twig`

**Interfaces:**
- Consumes: `t()` Twig function
- Produces: 22 `categories.*` translation keys; both category templates use `t()` for all visible strings

- [ ] **Step 1: Add `categories.*` keys to all 5 lang files**

Add after the last `products.*` key in each file (add a comma after `"products.form.cancel": "..."` first).

**cs.json:**
```json
  "categories.title": "Kategorie",
  "categories.add": "+ Přidat kategorii",
  "categories.col.id": "ID",
  "categories.col.name": "Název",
  "categories.col.slug": "Slug",
  "categories.col.order": "Pořadí",
  "categories.col.actions": "Akce",
  "categories.edit": "Upravit",
  "categories.delete": "Smazat",
  "categories.confirm_delete": "Smazat?",
  "categories.no_categories": "Žádné kategorie.",
  "categories.form.title_new": "Nová kategorie",
  "categories.form.title_edit": "Upravit kategorii",
  "categories.form.back": "← Zpět",
  "categories.form.slug": "Slug (URL)",
  "categories.form.order": "Pořadí",
  "categories.form.translations": "Překlady",
  "categories.form.name_label": "Název",
  "categories.form.desc_label": "Popis",
  "categories.form.translate_btn": "Přeložit z češtiny",
  "categories.form.save": "Uložit",
  "categories.form.cancel": "Zrušit"
```

**en.json:**
```json
  "categories.title": "Categories",
  "categories.add": "+ Add category",
  "categories.col.id": "ID",
  "categories.col.name": "Name",
  "categories.col.slug": "Slug",
  "categories.col.order": "Order",
  "categories.col.actions": "Actions",
  "categories.edit": "Edit",
  "categories.delete": "Delete",
  "categories.confirm_delete": "Delete?",
  "categories.no_categories": "No categories.",
  "categories.form.title_new": "New category",
  "categories.form.title_edit": "Edit category",
  "categories.form.back": "← Back",
  "categories.form.slug": "Slug (URL)",
  "categories.form.order": "Order",
  "categories.form.translations": "Translations",
  "categories.form.name_label": "Name",
  "categories.form.desc_label": "Description",
  "categories.form.translate_btn": "Translate from Czech",
  "categories.form.save": "Save",
  "categories.form.cancel": "Cancel"
```

**ru.json:**
```json
  "categories.title": "Категории",
  "categories.add": "+ Добавить категорию",
  "categories.col.id": "ID",
  "categories.col.name": "Название",
  "categories.col.slug": "Slug",
  "categories.col.order": "Порядок",
  "categories.col.actions": "Действия",
  "categories.edit": "Изменить",
  "categories.delete": "Удалить",
  "categories.confirm_delete": "Удалить?",
  "categories.no_categories": "Нет категорий.",
  "categories.form.title_new": "Новая категория",
  "categories.form.title_edit": "Изменить категорию",
  "categories.form.back": "← Назад",
  "categories.form.slug": "Slug (URL)",
  "categories.form.order": "Порядок",
  "categories.form.translations": "Переводы",
  "categories.form.name_label": "Название",
  "categories.form.desc_label": "Описание",
  "categories.form.translate_btn": "Перевести с чешского",
  "categories.form.save": "Сохранить",
  "categories.form.cancel": "Отмена"
```

**sk.json:**
```json
  "categories.title": "Kategórie",
  "categories.add": "+ Pridať kategóriu",
  "categories.col.id": "ID",
  "categories.col.name": "Názov",
  "categories.col.slug": "Slug",
  "categories.col.order": "Poradie",
  "categories.col.actions": "Akcie",
  "categories.edit": "Upraviť",
  "categories.delete": "Zmazať",
  "categories.confirm_delete": "Zmazať?",
  "categories.no_categories": "Žiadne kategórie.",
  "categories.form.title_new": "Nová kategória",
  "categories.form.title_edit": "Upraviť kategóriu",
  "categories.form.back": "← Späť",
  "categories.form.slug": "Slug (URL)",
  "categories.form.order": "Poradie",
  "categories.form.translations": "Preklady",
  "categories.form.name_label": "Názov",
  "categories.form.desc_label": "Popis",
  "categories.form.translate_btn": "Preložiť z češtiny",
  "categories.form.save": "Uložiť",
  "categories.form.cancel": "Zrušiť"
```

**uk.json:**
```json
  "categories.title": "Категорії",
  "categories.add": "+ Додати категорію",
  "categories.col.id": "ID",
  "categories.col.name": "Назва",
  "categories.col.slug": "Slug",
  "categories.col.order": "Порядок",
  "categories.col.actions": "Дії",
  "categories.edit": "Редагувати",
  "categories.delete": "Видалити",
  "categories.confirm_delete": "Видалити?",
  "categories.no_categories": "Немає категорій.",
  "categories.form.title_new": "Нова категорія",
  "categories.form.title_edit": "Редагувати категорію",
  "categories.form.back": "← Назад",
  "categories.form.slug": "Slug (URL)",
  "categories.form.order": "Порядок",
  "categories.form.translations": "Переклади",
  "categories.form.name_label": "Назва",
  "categories.form.desc_label": "Опис",
  "categories.form.translate_btn": "Перекласти з чеської",
  "categories.form.save": "Зберегти",
  "categories.form.cancel": "Скасувати"
```

- [ ] **Step 2: Verify all files have 132 keys and valid JSON**

```bash
for f in lang/admin/*.json; do echo -n "$f: "; php -r "echo count(json_decode(file_get_contents('$f'), true)) . ' keys\n';"; done
```

Expected:
```
lang/admin/cs.json: 132 keys
lang/admin/en.json: 132 keys
lang/admin/ru.json: 132 keys
lang/admin/sk.json: 132 keys
lang/admin/uk.json: 132 keys
```

- [ ] **Step 3: Replace templates/admin/categories/index.twig**

Full new content:
```twig
{% extends "layout/admin-base.twig" %}
{% block title %}{{ t('categories.title') }}{% endblock %}
{% block content %}
<div class="admin-topbar">
    <h1>{{ t('categories.title') }}</h1>
    <a href="/admin/categories/new" class="btn btn-primary">{{ t('categories.add') }}</a>
</div>
<table class="admin-table">
    <thead>
        <tr>
            <th>{{ t('categories.col.id') }}</th>
            <th>{{ t('categories.col.name') }}</th>
            <th>{{ t('categories.col.slug') }}</th>
            <th>{{ t('categories.col.order') }}</th>
            <th>{{ t('categories.col.actions') }}</th>
        </tr>
    </thead>
    <tbody>
    {% for cat in categories %}
    <tr>
        <td>{{ cat.id }}</td>
        <td>{{ cat.name ?? '—' }}</td>
        <td>{{ cat.slug }}</td>
        <td>{{ cat.sort_order }}</td>
        <td>
            <a href="/admin/categories/{{ cat.id }}/edit">{{ t('categories.edit') }}</a> |
            <form method="POST" action="/admin/categories/{{ cat.id }}/delete" style="display:inline" onsubmit="return confirm('Smazat?')">
                <button class="btn-link">{{ t('categories.delete') }}</button>
            </form>
        </td>
    </tr>
    {% else %}
    <tr><td colspan="5">{{ t('categories.no_categories') }}</td></tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %}
```

Note: `onsubmit="return confirm('Smazat?')"` remains hardcoded Czech per spec.

- [ ] **Step 4: Replace templates/admin/categories/form.twig**

Full new content:
```twig
{% extends "layout/admin-base.twig" %}
{% block title %}{{ category ? t('categories.form.title_edit') : t('categories.form.title_new') }}{% endblock %}
{% block content %}
<div class="admin-topbar">
    <h1>{{ category ? t('categories.form.title_edit') : t('categories.form.title_new') }}</h1>
    <a href="/admin/categories" class="btn btn-secondary">{{ t('categories.form.back') }}</a>
</div>
<form method="POST" action="{{ category ? '/admin/categories/' ~ category.id ~ '/edit' : '/admin/categories/new' }}" class="admin-form">
    <div class="form-group">
        <label>{{ t('categories.form.slug') }}</label>
        <input type="text" name="slug" value="{{ category.slug ?? '' }}" required>
    </div>
    <div class="form-group">
        <label>{{ t('categories.form.order') }}</label>
        <input type="number" name="sort_order" value="{{ category.sort_order ?? 0 }}">
    </div>
    <h3>{{ t('categories.form.translations') }}</h3>
    {% set lang_labels = {cs: 'CZ', sk: 'SK', en: 'EN', uk: 'UA', ru: 'RU'} %}
    <div class="lang-tabs">
        {% for lang in langs %}
        <button type="button" class="lang-tab {% if loop.first %}active{% endif %}" data-lang="{{ lang }}">{{ lang_labels[lang] ?? lang|upper }}</button>
        {% endfor %}
    </div>
    {% for lang in langs %}
    <div class="lang-panel {% if loop.first %}active{% endif %}" id="panel-{{ lang }}">
        <div class="form-group">
            <label>{{ t('categories.form.name_label') }} ({{ lang_labels[lang] ?? lang|upper }})</label>
            <input type="text" name="t[{{ lang }}][name]" value="{{ translations[lang].name ?? '' }}">
        </div>
        <div class="form-group">
            <label>{{ t('categories.form.desc_label') }} ({{ lang_labels[lang] ?? lang|upper }})</label>
            <textarea name="t[{{ lang }}][description]">{{ translations[lang].description ?? '' }}</textarea>
        </div>
        {% if lang != 'cs' %}
        <div class="form-group">
            <button type="button" class="btn btn-secondary translate-btn" data-lang="{{ lang }}">{{ t('categories.form.translate_btn') }}</button>
            <span class="translate-msg" style="display:none;margin-left:0.5rem;font-size:0.85rem;"></span>
        </div>
        {% endif %}
    </div>
    {% endfor %}
    <div class="form-actions">
        <button type="submit" class="btn btn-primary">{{ t('categories.form.save') }}</button>
        <a href="/admin/categories" class="btn btn-secondary">{{ t('categories.form.cancel') }}</a>
    </div>
</form>
{% block scripts %}
<script>
// Tab switching
document.querySelectorAll('.lang-tab').forEach(tab => {
    tab.addEventListener('click', () => {
        document.querySelectorAll('.lang-tab').forEach(t => t.classList.remove('active'));
        document.querySelectorAll('.lang-panel').forEach(p => p.classList.remove('active'));
        tab.classList.add('active');
        document.getElementById('panel-' + tab.dataset.lang).classList.add('active');
    });
});

// Slug auto-generation from CZ name
(function () {
    const slugInput = document.querySelector('input[name="slug"]');
    const csName    = document.querySelector('input[name="t[cs][name]"]');
    if (!slugInput || !csName) return;

    function toSlug(s) {
        return s.toLowerCase()
            .replace(/[áä]/g,'a').replace(/č/g,'c').replace(/ď/g,'d')
            .replace(/[éě]/g,'e').replace(/í/g,'i').replace(/ň/g,'n')
            .replace(/[óö]/g,'o').replace(/[řŕ]/g,'r').replace(/š/g,'s')
            .replace(/ť/g,'t').replace(/[úůü]/g,'u').replace(/ý/g,'y')
            .replace(/ž/g,'z')
            .replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'');
    }

    let locked = slugInput.value !== '';
    slugInput.addEventListener('input', () => { locked = slugInput.value !== ''; });
    csName.addEventListener('input', () => { if (!locked) slugInput.value = toSlug(csName.value); });
})();

// Translate buttons — JS runtime strings remain hardcoded Czech per spec
document.querySelectorAll('.translate-btn').forEach(btn => {
    btn.addEventListener('click', async () => {
        const targetLang = btn.dataset.lang;
        const panel      = document.getElementById('panel-' + targetLang);
        const msgSpan    = panel.querySelector('.translate-msg');
        const csName     = document.querySelector('input[name="t[cs][name]"]');
        const csDesc     = document.querySelector('textarea[name="t[cs][description]"]');

        if (!csName.value.trim() && !csDesc.value.trim()) {
            msgSpan.textContent = 'Nejprve vyplňte český název.';
            msgSpan.style.color = '#c00';
            msgSpan.style.display = 'inline';
            return;
        }

        btn.disabled    = true;
        btn.textContent = 'Překládám…';
        msgSpan.style.display = 'none';
        msgSpan.textContent   = '';

        try {
            const res = await fetch('/admin/translate', {
                method:  'POST',
                headers: {'Content-Type': 'application/json'},
                body:    JSON.stringify({
                    texts:  [csName.value, csDesc.value],
                    target: targetLang.toUpperCase(),
                }),
            });

            const data = await res.json();

            if (!res.ok || data.error) {
                msgSpan.textContent   = 'Překlad se nezdařil: ' + (data.error ?? res.status);
                msgSpan.style.color   = '#c00';
                msgSpan.style.display = 'inline';
                return;
            }

            panel.querySelector('input[name="t[' + targetLang + '][name]"]').value           = data.texts[0] ?? '';
            panel.querySelector('textarea[name="t[' + targetLang + '][description]"]').value = data.texts[1] ?? '';
            msgSpan.style.display = 'none';
            msgSpan.textContent   = '';
        } catch (e) {
            msgSpan.textContent   = 'Překlad se nezdařil: ' + e.message;
            msgSpan.style.color   = '#c00';
            msgSpan.style.display = 'inline';
        } finally {
            btn.disabled    = false;
            btn.textContent = 'Přeložit z češtiny';
        }
    });
});
</script>
{% endblock %}
{% endblock %}
```

- [ ] **Step 5: Smoke-test in the browser**

1. Switch to **EN** via the lang switcher.
2. Visit `/admin/categories` — verify: "Categories" heading, "+ Add category" button, column headers "ID", "Name", "Slug", "Order", "Actions", "Edit" link, "Delete" button, "No categories." empty state.
3. Visit `/admin/categories/new` — verify: "New category" heading, "← Back", "Slug (URL)", "Order", "Translations", "Name (CZ)", "Description (CZ)" labels, "Translate from Czech" button, "Save" and "Cancel" buttons.
4. Switch to **SK** — verify Slovak labels including "Preložiť z češtiny" button initial render.

- [ ] **Step 6: Run full test suite**

```bash
php vendor/bin/phpunit --testdox
```

Expected: all existing tests pass (no new tests for template changes).

- [ ] **Step 7: Commit**

```bash
git add lang/admin/cs.json lang/admin/en.json lang/admin/ru.json lang/admin/sk.json lang/admin/uk.json templates/admin/categories/index.twig templates/admin/categories/form.twig
git commit -m "feat: translate admin categories section"
```

---

## Iteration 4 — Orders, Gallery + key sort

> Tasks 1–12 above are complete. Tasks 13–14 extend the same pattern to Orders and Gallery, and also sort all keys alphabetically as a one-time housekeeping step.
> After Task 14 each lang file will have 182 keys (132 existing + 27 orders + 23 gallery).

---

## Task 13: Orders i18n + key sort

**Files:**
- Modify: `lang/admin/cs.json`
- Modify: `lang/admin/en.json`
- Modify: `lang/admin/ru.json`
- Modify: `lang/admin/sk.json`
- Modify: `lang/admin/uk.json`
- Modify: `templates/admin/orders/index.twig`
- Modify: `templates/admin/orders/detail.twig`

**Interfaces:**
- Consumes: existing `t()` wiring (AdminLangMiddleware + AdminBaseController)
- Produces: all 5 lang files sorted alphabetically by key with 159 keys each; both order templates use `t()` for all visible strings

- [ ] **Step 1: Sort all 5 lang files alphabetically by key**

Run this PHP one-liner from the project root:

```bash
php -r "
foreach (glob('lang/admin/*.json') as \$f) {
    \$data = json_decode(file_get_contents(\$f), true);
    ksort(\$data);
    \$entries = [];
    foreach (\$data as \$k => \$v) {
        \$entries[] = '  ' . json_encode(\$k, JSON_UNESCAPED_UNICODE) . ': ' . json_encode(\$v, JSON_UNESCAPED_UNICODE);
    }
    file_put_contents(\$f, \"{\n\" . implode(\",\n\", \$entries) . \"\n}\n\");
}
echo 'Sorted.' . PHP_EOL;
"
```

After sorting, the section order (by key prefix) is: `blog.*` → `categories.*` → `dashboard.*` → `nav.*` → `products.*` → `settings.*` → `users.*`

- [ ] **Step 2: Verify sort — 132 keys each, valid JSON**

```bash
for f in lang/admin/*.json; do echo -n "$f: "; php -r "echo count(json_decode(file_get_contents('$f'), true)) . ' keys\n';"; done
for f in lang/admin/*.json; do php -r "json_decode(file_get_contents('$f')); echo json_last_error() == 0 ? '$f OK\n' : '$f ERROR\n';"; done
```

Expected: 132 keys each, all OK.

- [ ] **Step 3: Add `orders.*` keys to all 5 lang files**

The `orders.*` keys sort between `nav.*` and `products.*`. For each file, open it and append the following block before the closing `}` (after the last `nav.*` entry, in the correct sorted position — or simply append at the end, since the next step re-sorts nothing; the sort in Step 1 already ordered the existing keys, and these new keys will be in the right alphabetical section). In practice: add a comma after the last key, then insert the new block before `}`.

**cs.json — add these 27 keys:**
```json
  "orders.col.created": "Vytvořena",
  "orders.col.customer": "Zákazník",
  "orders.col.email": "E-mail",
  "orders.col.number": "Číslo",
  "orders.col.status": "Status",
  "orders.col.total": "Celkem",
  "orders.detail.back": "← Zpět",
  "orders.detail.change_status": "Změnit status",
  "orders.detail.col.product": "Produkt",
  "orders.detail.col.qty": "Ks",
  "orders.detail.col.total": "Celkem",
  "orders.detail.col.unit_price": "Cena/ks",
  "orders.detail.created": "Vytvořena:",
  "orders.detail.customer": "Zákazník",
  "orders.detail.email": "E-mail:",
  "orders.detail.gopay_id": "GoPay ID:",
  "orders.detail.name": "Jméno:",
  "orders.detail.notes": "Poznámka:",
  "orders.detail.phone": "Telefon:",
  "orders.detail.pickup_date": "Datum vyzvednutí:",
  "orders.detail.save": "Uložit",
  "orders.detail.title": "Objednávka",
  "orders.detail_link": "Detail →",
  "orders.filter": "Filtrovat:",
  "orders.filter_all": "— všechny —",
  "orders.no_orders": "Žádné objednávky.",
  "orders.title": "Objednávky"
```

**en.json — add these 27 keys:**
```json
  "orders.col.created": "Created",
  "orders.col.customer": "Customer",
  "orders.col.email": "E-mail",
  "orders.col.number": "Number",
  "orders.col.status": "Status",
  "orders.col.total": "Total",
  "orders.detail.back": "← Back",
  "orders.detail.change_status": "Change status",
  "orders.detail.col.product": "Product",
  "orders.detail.col.qty": "Qty",
  "orders.detail.col.total": "Total",
  "orders.detail.col.unit_price": "Unit price",
  "orders.detail.created": "Created:",
  "orders.detail.customer": "Customer",
  "orders.detail.email": "E-mail:",
  "orders.detail.gopay_id": "GoPay ID:",
  "orders.detail.name": "Name:",
  "orders.detail.notes": "Notes:",
  "orders.detail.phone": "Phone:",
  "orders.detail.pickup_date": "Pickup date:",
  "orders.detail.save": "Save",
  "orders.detail.title": "Order",
  "orders.detail_link": "Detail →",
  "orders.filter": "Filter:",
  "orders.filter_all": "— all —",
  "orders.no_orders": "No orders.",
  "orders.title": "Orders"
```

**ru.json — add these 27 keys:**
```json
  "orders.col.created": "Создан",
  "orders.col.customer": "Клиент",
  "orders.col.email": "E-mail",
  "orders.col.number": "Номер",
  "orders.col.status": "Статус",
  "orders.col.total": "Итого",
  "orders.detail.back": "← Назад",
  "orders.detail.change_status": "Изменить статус",
  "orders.detail.col.product": "Товар",
  "orders.detail.col.qty": "Кол.",
  "orders.detail.col.total": "Итого",
  "orders.detail.col.unit_price": "Цена/шт.",
  "orders.detail.created": "Создан:",
  "orders.detail.customer": "Клиент",
  "orders.detail.email": "E-mail:",
  "orders.detail.gopay_id": "GoPay ID:",
  "orders.detail.name": "Имя:",
  "orders.detail.notes": "Примечание:",
  "orders.detail.phone": "Телефон:",
  "orders.detail.pickup_date": "Дата получения:",
  "orders.detail.save": "Сохранить",
  "orders.detail.title": "Заказ",
  "orders.detail_link": "Детали →",
  "orders.filter": "Фильтр:",
  "orders.filter_all": "— все —",
  "orders.no_orders": "Нет заказов.",
  "orders.title": "Заказы"
```

**sk.json — add these 27 keys:**
```json
  "orders.col.created": "Vytvorená",
  "orders.col.customer": "Zákazník",
  "orders.col.email": "E-mail",
  "orders.col.number": "Číslo",
  "orders.col.status": "Stav",
  "orders.col.total": "Celkom",
  "orders.detail.back": "← Späť",
  "orders.detail.change_status": "Zmeniť stav",
  "orders.detail.col.product": "Produkt",
  "orders.detail.col.qty": "Ks",
  "orders.detail.col.total": "Celkom",
  "orders.detail.col.unit_price": "Cena/ks",
  "orders.detail.created": "Vytvorená:",
  "orders.detail.customer": "Zákazník",
  "orders.detail.email": "E-mail:",
  "orders.detail.gopay_id": "GoPay ID:",
  "orders.detail.name": "Meno:",
  "orders.detail.notes": "Poznámka:",
  "orders.detail.phone": "Telefón:",
  "orders.detail.pickup_date": "Dátum vyzdvihnutia:",
  "orders.detail.save": "Uložiť",
  "orders.detail.title": "Objednávka",
  "orders.detail_link": "Detail →",
  "orders.filter": "Filtrovať:",
  "orders.filter_all": "— všetky —",
  "orders.no_orders": "Žiadne objednávky.",
  "orders.title": "Objednávky"
```

**uk.json — add these 27 keys:**
```json
  "orders.col.created": "Створено",
  "orders.col.customer": "Замовник",
  "orders.col.email": "E-mail",
  "orders.col.number": "Номер",
  "orders.col.status": "Статус",
  "orders.col.total": "Разом",
  "orders.detail.back": "← Назад",
  "orders.detail.change_status": "Змінити статус",
  "orders.detail.col.product": "Товар",
  "orders.detail.col.qty": "Кіл.",
  "orders.detail.col.total": "Разом",
  "orders.detail.col.unit_price": "Ціна/шт.",
  "orders.detail.created": "Створено:",
  "orders.detail.customer": "Замовник",
  "orders.detail.email": "E-mail:",
  "orders.detail.gopay_id": "GoPay ID:",
  "orders.detail.name": "Ім'я:",
  "orders.detail.notes": "Примітка:",
  "orders.detail.phone": "Телефон:",
  "orders.detail.pickup_date": "Дата отримання:",
  "orders.detail.save": "Зберегти",
  "orders.detail.title": "Замовлення",
  "orders.detail_link": "Деталі →",
  "orders.filter": "Фільтр:",
  "orders.filter_all": "— всі —",
  "orders.no_orders": "Немає замовлень.",
  "orders.title": "Замовлення"
```

- [ ] **Step 4: Verify all files have 159 keys and valid JSON**

```bash
for f in lang/admin/*.json; do echo -n "$f: "; php -r "echo count(json_decode(file_get_contents('$f'), true)) . ' keys\n';"; done
for f in lang/admin/*.json; do php -r "json_decode(file_get_contents('$f')); echo json_last_error() == 0 ? '$f OK\n' : '$f ERROR\n';"; done
```

Expected:
```
lang/admin/cs.json: 159 keys
lang/admin/en.json: 159 keys
lang/admin/ru.json: 159 keys
lang/admin/sk.json: 159 keys
lang/admin/uk.json: 159 keys
```

- [ ] **Step 5: Replace `templates/admin/orders/index.twig`**

Read the current file first, then write:

```twig
{% extends "layout/admin-base.twig" %}
{% block title %}{{ t('orders.title') }}{% endblock %}
{% block content %}
<div class="admin-topbar"><h1>{{ t('orders.title') }}</h1></div>
<form method="GET" action="/admin/orders" style="margin-bottom:1rem;display:flex;gap:0.5rem;align-items:center;">
    <label>{{ t('orders.filter') }}</label>
    <select name="status" onchange="this.form.submit()">
        <option value="">{{ t('orders.filter_all') }}</option>
        {% for s in statuses %}
        <option value="{{ s }}" {% if s == status %}selected{% endif %}>{{ s }}</option>
        {% endfor %}
    </select>
</form>
<table class="admin-table">
    <thead><tr><th>{{ t('orders.col.number') }}</th><th>{{ t('orders.col.customer') }}</th><th>{{ t('orders.col.email') }}</th><th>{{ t('orders.col.total') }}</th><th>{{ t('orders.col.status') }}</th><th>{{ t('orders.col.created') }}</th><th></th></tr></thead>
    <tbody>
    {% for o in orders %}
    <tr>
        <td><a href="/admin/orders/{{ o.order_number }}">{{ o.order_number }}</a></td>
        <td>{{ o.customer_name }}</td>
        <td>{{ o.customer_email }}</td>
        <td>{{ o.total_amount|number_format(2, '.', ' ') }} Kč</td>
        <td><span class="badge badge-{{ o.status }}">{{ o.status }}</span></td>
        <td>{{ o.created_at }}</td>
        <td><a href="/admin/orders/{{ o.order_number }}">{{ t('orders.detail_link') }}</a></td>
    </tr>
    {% else %}
    <tr><td colspan="7">{{ t('orders.no_orders') }}</td></tr>
    {% endfor %}
    </tbody>
</table>
{% if pages > 1 %}
<div class="pagination" style="margin-top:1rem;">
    {% for p in 1..pages %}
    <a href="?page={{ p }}{% if status %}&status={{ status }}{% endif %}" class="{% if p == page %}active{% endif %}">{{ p }}</a>
    {% endfor %}
</div>
{% endif %}
{% endblock %}
```

- [ ] **Step 6: Replace `templates/admin/orders/detail.twig`**

Read the current file first, then write:

```twig
{% extends "layout/admin-base.twig" %}
{% block title %}{{ t('orders.detail.title') }} {{ order.order_number }}{% endblock %}
{% block content %}
<div class="admin-topbar">
    <h1>{{ t('orders.detail.title') }} {{ order.order_number }}</h1>
    <a href="/admin/orders" class="btn btn-secondary">{{ t('orders.detail.back') }}</a>
</div>
<div style="display:grid;grid-template-columns:1fr 1fr;gap:2rem;">
<div class="admin-form">
    <h3>{{ t('orders.detail.customer') }}</h3>
    <p><strong>{{ t('orders.detail.name') }}</strong> {{ order.customer_name }}</p>
    <p><strong>{{ t('orders.detail.email') }}</strong> {{ order.customer_email }}</p>
    <p><strong>{{ t('orders.detail.phone') }}</strong> {{ order.customer_phone }}</p>
    <p><strong>{{ t('orders.detail.pickup_date') }}</strong> {{ order.pickup_date ?? '—' }}</p>
    <p><strong>{{ t('orders.detail.notes') }}</strong> {{ order.notes ?? '—' }}</p>
    <p><strong>{{ t('orders.detail.gopay_id') }}</strong> {{ order.gopay_payment_id ?? '—' }}</p>
    <p><strong>{{ t('orders.detail.created') }}</strong> {{ order.created_at }}</p>
    <hr style="margin:1rem 0;">
    <h3>{{ t('orders.detail.change_status') }}</h3>
    <form method="POST" action="/admin/orders/{{ order.order_number }}/status" style="display:flex;gap:0.5rem;align-items:center;">
        <select name="status">
            {% for s in statuses %}
            <option value="{{ s }}" {% if s == order.status %}selected{% endif %}>{{ s }}</option>
            {% endfor %}
        </select>
        <button type="submit" class="btn btn-primary">{{ t('orders.detail.save') }}</button>
    </form>
</div>
<div>
    <table class="admin-table">
        <thead><tr><th>{{ t('orders.detail.col.product') }}</th><th>{{ t('orders.detail.col.qty') }}</th><th>{{ t('orders.detail.col.unit_price') }}</th><th>{{ t('orders.detail.col.total') }}</th></tr></thead>
        <tbody>
        {% for item in order.items %}
        <tr>
            <td>{{ item.product_name_snapshot }}</td>
            <td>{{ item.quantity }}</td>
            <td>{{ item.unit_price|number_format(2, '.', ' ') }} Kč</td>
            <td>{{ (item.unit_price * item.quantity)|number_format(2, '.', ' ') }} Kč</td>
        </tr>
        {% endfor %}
        </tbody>
    </table>
    <div class="summary-total" style="margin-top:0.5rem;">
        <span>{{ t('orders.detail.col.total') }}</span>
        <strong>{{ order.total_amount|number_format(2, '.', ' ') }} Kč</strong>
    </div>
</div>
</div>
{% endblock %}
```

Note: `orders.detail.col.total` is used twice — for the table column header and the summary label. This is intentional; both say "Celkem"/"Total".

- [ ] **Step 7: Smoke-test in the browser**

Start server if not running:
```bash
php -S localhost:8080 -t www
```

1. Switch to **EN** via the lang switcher.
2. Visit `/admin/orders` — verify: "Orders" heading, "Filter:" label, "— all —" option, column headers "Number", "Customer", "E-mail", "Total", "Status", "Created", "Detail →" link, "No orders." empty state (if no orders exist).
3. If an order exists, click "Detail →" — verify: "Order ORD-XXXX" title, "← Back", "Customer" heading, all field labels in English, "Change status" heading, "Save" button, item table headers "Product", "Qty", "Unit price", "Total", summary "Total".
4. Switch to **RU** — verify Cyrillic labels.

- [ ] **Step 8: Commit**

```bash
git add lang/admin/cs.json lang/admin/en.json lang/admin/ru.json lang/admin/sk.json lang/admin/uk.json templates/admin/orders/index.twig templates/admin/orders/detail.twig
git commit -m "feat: translate admin orders section and sort lang keys alphabetically"
```

---

## Task 14: Gallery i18n

**Files:**
- Modify: `lang/admin/cs.json`
- Modify: `lang/admin/en.json`
- Modify: `lang/admin/ru.json`
- Modify: `lang/admin/sk.json`
- Modify: `lang/admin/uk.json`
- Modify: `templates/admin/gallery/index.twig`
- Modify: `templates/admin/gallery/form.twig`

**Interfaces:**
- Consumes: `t()` Twig function (already wired)
- Produces: 23 `gallery.*` keys in all 5 lang files; both gallery templates use `t()` for all Twig-rendered strings

- [ ] **Step 1: Add `gallery.*` keys to all 5 lang files**

The `gallery.*` keys sort between `dashboard.*` and `nav.*`. Add after the last existing key in each file (add comma after last key, then insert before closing `}`).

**cs.json — add these 23 keys:**
```json
  "gallery.add": "+ Přidat album",
  "gallery.col.actions": "Akce",
  "gallery.col.id": "ID",
  "gallery.col.order": "Pořadí",
  "gallery.col.slug": "Slug",
  "gallery.confirm_delete": "Smazat album i se všemi fotkami?",
  "gallery.delete": "Smazat",
  "gallery.edit": "Upravit",
  "gallery.form.add_photos": "Přidat fotky",
  "gallery.form.back": "← Zpět",
  "gallery.form.cancel": "Zrušit",
  "gallery.form.delete_photo": "Smazat",
  "gallery.form.desc_label": "Popis",
  "gallery.form.existing_photos": "Fotky v albu",
  "gallery.form.name_label": "Název alba",
  "gallery.form.order": "Pořadí",
  "gallery.form.save": "Uložit",
  "gallery.form.slug": "Slug",
  "gallery.form.title_edit": "Upravit album",
  "gallery.form.title_new": "Nové album",
  "gallery.form.translations": "Překlady",
  "gallery.no_albums": "Žádná alba.",
  "gallery.title": "Galerie"
```

**en.json — add these 23 keys:**
```json
  "gallery.add": "+ Add album",
  "gallery.col.actions": "Actions",
  "gallery.col.id": "ID",
  "gallery.col.order": "Order",
  "gallery.col.slug": "Slug",
  "gallery.confirm_delete": "Delete album with all photos?",
  "gallery.delete": "Delete",
  "gallery.edit": "Edit",
  "gallery.form.add_photos": "Add photos",
  "gallery.form.back": "← Back",
  "gallery.form.cancel": "Cancel",
  "gallery.form.delete_photo": "Delete",
  "gallery.form.desc_label": "Description",
  "gallery.form.existing_photos": "Album photos",
  "gallery.form.name_label": "Album name",
  "gallery.form.order": "Order",
  "gallery.form.save": "Save",
  "gallery.form.slug": "Slug",
  "gallery.form.title_edit": "Edit album",
  "gallery.form.title_new": "New album",
  "gallery.form.translations": "Translations",
  "gallery.no_albums": "No albums.",
  "gallery.title": "Gallery"
```

**ru.json — add these 23 keys:**
```json
  "gallery.add": "+ Добавить альбом",
  "gallery.col.actions": "Действия",
  "gallery.col.id": "ID",
  "gallery.col.order": "Порядок",
  "gallery.col.slug": "Slug",
  "gallery.confirm_delete": "Удалить альбом со всеми фото?",
  "gallery.delete": "Удалить",
  "gallery.edit": "Изменить",
  "gallery.form.add_photos": "Добавить фото",
  "gallery.form.back": "← Назад",
  "gallery.form.cancel": "Отмена",
  "gallery.form.delete_photo": "Удалить",
  "gallery.form.desc_label": "Описание",
  "gallery.form.existing_photos": "Фото в альбоме",
  "gallery.form.name_label": "Название альбома",
  "gallery.form.order": "Порядок",
  "gallery.form.save": "Сохранить",
  "gallery.form.slug": "Slug",
  "gallery.form.title_edit": "Изменить альбом",
  "gallery.form.title_new": "Новый альбом",
  "gallery.form.translations": "Переводы",
  "gallery.no_albums": "Нет альбомов.",
  "gallery.title": "Галерея"
```

**sk.json — add these 23 keys:**
```json
  "gallery.add": "+ Pridať album",
  "gallery.col.actions": "Akcie",
  "gallery.col.id": "ID",
  "gallery.col.order": "Poradie",
  "gallery.col.slug": "Slug",
  "gallery.confirm_delete": "Zmazať album aj so všetkými fotkami?",
  "gallery.delete": "Zmazať",
  "gallery.edit": "Upraviť",
  "gallery.form.add_photos": "Pridať fotky",
  "gallery.form.back": "← Späť",
  "gallery.form.cancel": "Zrušiť",
  "gallery.form.delete_photo": "Zmazať",
  "gallery.form.desc_label": "Popis",
  "gallery.form.existing_photos": "Fotky v albume",
  "gallery.form.name_label": "Názov albumu",
  "gallery.form.order": "Poradie",
  "gallery.form.save": "Uložiť",
  "gallery.form.slug": "Slug",
  "gallery.form.title_edit": "Upraviť album",
  "gallery.form.title_new": "Nový album",
  "gallery.form.translations": "Preklady",
  "gallery.no_albums": "Žiadne albumy.",
  "gallery.title": "Galéria"
```

**uk.json — add these 23 keys:**
```json
  "gallery.add": "+ Додати альбом",
  "gallery.col.actions": "Дії",
  "gallery.col.id": "ID",
  "gallery.col.order": "Порядок",
  "gallery.col.slug": "Slug",
  "gallery.confirm_delete": "Видалити альбом з усіма фото?",
  "gallery.delete": "Видалити",
  "gallery.edit": "Редагувати",
  "gallery.form.add_photos": "Додати фото",
  "gallery.form.back": "← Назад",
  "gallery.form.cancel": "Скасувати",
  "gallery.form.delete_photo": "Видалити",
  "gallery.form.desc_label": "Опис",
  "gallery.form.existing_photos": "Фото в альбомі",
  "gallery.form.name_label": "Назва альбому",
  "gallery.form.order": "Порядок",
  "gallery.form.save": "Зберегти",
  "gallery.form.slug": "Slug",
  "gallery.form.title_edit": "Редагувати альбом",
  "gallery.form.title_new": "Новий альбом",
  "gallery.form.translations": "Переклади",
  "gallery.no_albums": "Немає альбомів.",
  "gallery.title": "Галерея"
```

- [ ] **Step 2: Verify all files have 182 keys and valid JSON**

```bash
for f in lang/admin/*.json; do echo -n "$f: "; php -r "echo count(json_decode(file_get_contents('$f'), true)) . ' keys\n';"; done
for f in lang/admin/*.json; do php -r "json_decode(file_get_contents('$f')); echo json_last_error() == 0 ? '$f OK\n' : '$f ERROR\n';"; done
```

Expected:
```
lang/admin/cs.json: 182 keys
lang/admin/en.json: 182 keys
lang/admin/ru.json: 182 keys
lang/admin/sk.json: 182 keys
lang/admin/uk.json: 182 keys
```

- [ ] **Step 3: Replace `templates/admin/gallery/index.twig`**

Read the current file first, then write:

```twig
{% extends "layout/admin-base.twig" %}
{% block title %}{{ t('gallery.title') }}{% endblock %}
{% block content %}
<div class="admin-topbar">
    <h1>{{ t('gallery.title') }}</h1>
    <a href="/admin/gallery/new" class="btn btn-primary">{{ t('gallery.add') }}</a>
</div>
<table class="admin-table">
    <thead><tr><th>{{ t('gallery.col.id') }}</th><th>{{ t('gallery.col.slug') }}</th><th>{{ t('gallery.col.order') }}</th><th>{{ t('gallery.col.actions') }}</th></tr></thead>
    <tbody>
    {% for a in albums %}
    <tr>
        <td>{{ a.id }}</td>
        <td>{{ a.slug }}</td>
        <td>{{ a.sort_order }}</td>
        <td>
            <a href="/admin/gallery/{{ a.id }}/edit">{{ t('gallery.edit') }}</a> |
            <form method="POST" action="/admin/gallery/{{ a.id }}/delete" style="display:inline" onsubmit="return confirm('Smazat album i se všemi fotkami?')">
                <button class="btn-link">{{ t('gallery.delete') }}</button>
            </form>
        </td>
    </tr>
    {% else %}
    <tr><td colspan="4">{{ t('gallery.no_albums') }}</td></tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %}
```

Note: `onsubmit="return confirm('Smazat album i se všemi fotkami?')"` remains hardcoded Czech per spec.

- [ ] **Step 4: Replace `templates/admin/gallery/form.twig`**

Read the current file first, then write:

```twig
{% extends "layout/admin-base.twig" %}
{% block title %}{{ album ? t('gallery.form.title_edit') : t('gallery.form.title_new') }}{% endblock %}
{% block content %}
<div class="admin-topbar">
    <h1>{{ album ? t('gallery.form.title_edit') : t('gallery.form.title_new') }}</h1>
    <a href="/admin/gallery" class="btn btn-secondary">{{ t('gallery.form.back') }}</a>
</div>
<form method="POST" action="{{ album ? '/admin/gallery/' ~ album.id ~ '/edit' : '/admin/gallery/new' }}" enctype="multipart/form-data" class="admin-form">
    <div class="form-group">
        <label>{{ t('gallery.form.slug') }}</label>
        <input type="text" name="slug" value="{{ album.slug ?? '' }}" required>
    </div>
    <div class="form-group">
        <label>{{ t('gallery.form.order') }}</label>
        <input type="number" name="sort_order" value="{{ album.sort_order ?? 0 }}">
    </div>
    {% if album and album.images %}
    <div class="form-group">
        <label>{{ t('gallery.form.existing_photos') }}</label>
        <div style="display:flex;gap:0.5rem;flex-wrap:wrap;margin-top:0.5rem;">
        {% for img in album.images %}
        <div style="text-align:center;">
            <img src="/assets/uploads/gallery/thumb_{{ img.filename }}" class="img-thumb"><br>
            <form method="POST" action="/admin/gallery/{{ album.id }}/image/{{ img.id }}/delete">
                <button class="btn-link" style="font-size:0.8rem">{{ t('gallery.form.delete_photo') }}</button>
            </form>
        </div>
        {% endfor %}
        </div>
    </div>
    {% endif %}
    <div class="form-group">
        <label>{{ t('gallery.form.add_photos') }}</label>
        <input type="file" name="images[]" accept="image/*" multiple>
    </div>
    <h3>{{ t('gallery.form.translations') }}</h3>
    <div class="lang-tabs">
        {% for lang in langs %}
        <button type="button" class="lang-tab {% if loop.first %}active{% endif %}" data-lang="{{ lang }}">{{ lang|upper }}</button>
        {% endfor %}
    </div>
    {% for lang in langs %}
    <div class="lang-panel {% if loop.first %}active{% endif %}" id="panel-{{ lang }}">
        <div class="form-group">
            <label>{{ t('gallery.form.name_label') }} ({{ lang|upper }})</label>
            <input type="text" name="t[{{ lang }}][name]" value="{{ translations[lang].name ?? '' }}">
        </div>
        <div class="form-group">
            <label>{{ t('gallery.form.desc_label') }} ({{ lang|upper }})</label>
            <textarea name="t[{{ lang }}][description]">{{ translations[lang].description ?? '' }}</textarea>
        </div>
    </div>
    {% endfor %}
    <div class="form-actions">
        <button type="submit" class="btn btn-primary">{{ t('gallery.form.save') }}</button>
        <a href="/admin/gallery" class="btn btn-secondary">{{ t('gallery.form.cancel') }}</a>
    </div>
</form>
{% block scripts %}
<script>
document.querySelectorAll('.lang-tab').forEach(tab => {
    tab.addEventListener('click', () => {
        document.querySelectorAll('.lang-tab').forEach(t => t.classList.remove('active'));
        document.querySelectorAll('.lang-panel').forEach(p => p.classList.remove('active'));
        tab.classList.add('active');
        document.getElementById('panel-' + tab.dataset.lang).classList.add('active');
    });
});
</script>
{% endblock %}
{% endblock %}
```

- [ ] **Step 5: Smoke-test in the browser**

1. Switch to **EN** via the lang switcher.
2. Visit `/admin/gallery` — verify: "Gallery" heading, "+ Add album" button, column headers "ID", "Slug", "Order", "Actions", "Edit" link, "Delete" button, "No albums." empty state.
3. Visit `/admin/gallery/new` — verify: "New album" heading, "← Back", "Slug" label, "Order" label, "Add photos" label, "Translations" heading, "Album name (CS)", "Description (CS)" labels, "Save" and "Cancel" buttons.
4. Switch to **SK** — verify Slovak labels.

- [ ] **Step 6: Run full test suite**

```bash
php vendor/bin/phpunit --testdox
```

Expected: 46 tests pass, 1 pre-existing failure (`PageModelTest::testFindReturnsPage` — `'Services'` vs `'Our Services'`, unrelated to i18n changes).

- [ ] **Step 7: Commit**

```bash
git add lang/admin/cs.json lang/admin/en.json lang/admin/ru.json lang/admin/sk.json lang/admin/uk.json templates/admin/gallery/index.twig templates/admin/gallery/form.twig
git commit -m "feat: translate admin gallery section"
```
